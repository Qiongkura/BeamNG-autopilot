# 本轮交接证据（计划 §8.4 八项）

对象：线通道数据层修复（T11）与冻结留出集（T10）。生成日期 2026-09-24。
**证据分级（计划 §8.1）**：本轮全部是 **第 2 类（原始传感器/图像回放）** 与 **第 1 类（纯组件确定性测试）**；
**没有第 3 类（Tech 闭环）** —— 因此下列任何数字都**不能**用来声称车辆行为改善。

---

## 1. 工作树、配置、哈希与 run 清单

| 项 | 值 |
| --- | --- |
| HEAD | `55f41594`（分支 `fix/round3-hardening-20260921`） |
| 已跟踪改动 | **0**（`git status --porcelain` 无 `M/A/D`） |
| 未跟踪（有意不提交） | `.git.bak-20260920/`、`.workbuddy-ai/`、`rescue-20260920/` |
| 有效开关 | `BEHAVIOUR_SWITCHES` 共 22 项，本轮 shell **全部未设**（继承模块默认）；`BEAMNG_LINE_REFINE`、`BEAMNG_LINE_CAND_GATE` 均走默认（前者 `off`，后者 `on`） |
| 模型哈希（sha256 前 16） | `v13b` `7c0075677d26257d`；`ft_t08` `2bca9a850c2f99fe`；**`ft_t11` `a4c6e07e1b53b935`** |
| 跨地图冻结清单 | `frozen_holdout_eastcoast.json` 帧 digest **`92116b54d717117d`**（`east_coast_usa`，25 帧，训练集无该地图帧） |
| 冻结清单 | `frozen_holdout_wide2.json` 文件 `35685e3dbc989fa5` / 帧 digest **`5af57693878615cf`**；`frozen_holdout_lines2.json` 文件 `0dd569e29ba20e4c` / 帧 digest `a0f229e0a5a4531f`（后者已在 §5.7 消耗） |
| 训练 run 清单 | `ident_probe_20260923/front_main`、`ident_material_20260923/front_main`、`holdout_town_20260923/front_main`、`holdout_lines_20260923/front_main`、`diverse_wide_20260924/front_main`、`diverse_town_20260924/front_main`、`_ft_probe5_{junction,straight,plain}`（静止采集各取前 5 帧）= **188 帧** |
| 控制权冲突 | 本轮**没有**接任何驾驶；生产的 `default_model_path()` 仍指向 `logs/m5_seg/seg_model/best.pt`，本轮的 `ft_t11` **未被任何驾驶路径引用**（可用 `grep -r seg_model_v13b_ft_t11 beamng_autopilot/` 复核为空） |

## 2. 覆盖、缺列与 UNKNOWN（分字段）

| 数据 | 覆盖 | UNKNOWN / 缺列 |
| --- | --- | --- |
| 冻结留出集 `wide2` | 25 帧；引擎漆线 **71 844 px**；`scored=25` | 无缺帧；`--save-annotation` 未开（材质级分析不可用，已登记） |
| 阶段评估 `tiered` 臂 | `production_mismatch=0`（生产一致） | 另两臂 `pixel/component` 为 legacy，`production_mismatch=25/25`（工具自报，不参与结论） |
| 候选身份（`ident_ft_holdout2`） | 候选 128 | **未确认 112**（无引擎线对应）；`on_paint` 9 |
| 候选身份（`marking_identity_straight`） | 候选 257 | **未确认 228**；`on_paint` 0 |
| 候选身份（`marking_identity_junction`） | 候选 163 | **未确认 163**（全部无对应） |
| 被拒的后处理精修 | 候选 45 | 45 全部未确认（该配置已否决） |
| 行标签来源 | 引擎调色板（`palette_version` 随采集落盘） | 引擎只渲染已知材质 → 褪色漆线与 `DRIVING_INSTRUCTIONS` 涂装未计（§5.4 单列） |

## 3. 强制刷新与新源消费

**本轮未测**：这是驾驶链（T03）的字段，而本轮只做离线模型评估，没有跑驾驶、没有遥测。
已登记的现成证据在 `docs/ROUND7_STAGE_A_PROGRESS.md` §6b 与阶段 C 记录里；本轮不以旧数字充当新证据。

## 4. 独立几何正反例、参考版本一致性与命令回执

| 项 | 本轮证据 |
| --- | --- |
| 独立几何 | T05 独立 oracle 与管线一致（p50 0.0000 m，见 `docs/GEOMETRY_BASELINE.md`）；本轮未改几何 |
| 参考版本一致性 | 阶段评估自报 **`production_mismatch=0`**（`tiered` 臂 == `full_predict`），即报告描述的是生产链；另有 T02 的 `lane_ref_geom_id == scene_ref_geom_id` 21/21 |
| 最终命令回执 | 驾驶链字段，本轮无驾驶 → **未测**；T04 的对照记录在 `docs/ROUND7_STAGE_A_PROGRESS.md` §2 |

## 5. 路面/边界门：真阳性、漏检、误停、错误放行

本轮的相关量是**感知层**（不是边界门）：

| 模型（冻结 `wide2`，tiered 生产臂） | raw IoU | final IoU | 连通域 | 路外线像素 |
| --- | --- | --- | --- | --- |
| `v13b` | 0.2681 | **0.254** | 135 | 90 219 |
| `ft_t11` | **0.5907** | **0.599** | **69** | **68 281** |

按 `7.2` 的感知必报项（已知区 precision/recall）：`wide2` 上 **precision 0.265 → 0.597、recall 0.942 → 0.987**；
`lines2` 上 0.175 → 0.573 / 0.924 → 0.928。**误停与错误放行本轮未测**（无驾驶）。

## 6. 性能与 deadline

| 项 | 值 |
| --- | --- |
| 训练成本 | 188 帧、3 轮 ≈ **32 s**（RTX 5070；单轮 5 s，首轮 23 s 含加载） |
| 推理成本 | 本轮未测单帧耗时/延迟；认知：两次前向（v13b 与 ft_t11）在冻结集上均**无超时** |
| deadline 违反 | 驾驶链字段 → **未测**；工具侧无超时（两次 stage_eval 均 exit 0） |
| 明确包含关系 | 本轮的比较对象是**同一 run 的同一批帧**，同一 `--arms` 路径、同一 `tiered` 约束臂 |

## 7. 碰撞、压线、出铺装、按停段分类的停车时长与有效推进

**全部未测**：本轮**没有驾驶**。最近一次驾驶证据仍是 `docs/ROUND7_STAGE_A_PROGRESS.md` §6/§6i/§6j 记录的
短跑与阶段 C 对照（那些结论的适用版本是当时的 HEAD，**不自动迁移到本轮**）。

## 8. 通过 / 失败 / 未测（按"假设 → 是否激活 → 输入 → 实测 → 支持/推翻什么 → 未知"）

| 假设 | 激活 | 可复现输入 | 实测 | 支持/推翻 | 未知 |
| --- | --- | --- | --- | --- | --- |
| 后处理精修（结构+极性无关离群）能修线通道精度 | 否（默认 `off`，仅 A/B 时开） | `ident_probe_*`、两份旧留出集 | 掩码 precision 0.156→0.216（路口）/0.194→0.243（路缘）；候选匹配 0.132→0.000（直线段） | **推翻**（候选层把有用候选删掉；一份留出集上 recall→0） | 是否在别的阈值组合下可用（未扫） |
| 候选并集的 CV 臂需要"细长+在路面"门 | 是（`BEAMNG_LINE_CAND_GATE` 默认 on） | 同批 20 帧 | 路外候选 176→138；匹配/零假设 4.8×→…；掩码支撑候选全保留 | **支持**（候选卫生改善，且不误伤掩码支撑候选） | 对训练/驾驶的净效果 |
| 线通道精度不足是数据层问题、可用同源标注微调修复 | 是（产出 `ft_t08`） | 158 帧同源标注 | `frozen_lines2` precision 0.175→**0.423**、recall 0.924→0.928 | **支持** | 场景外表现 |
| 微调的退化场景靠"场景多样化数据"消除 | 是（产出 `ft_t11`，188 帧） | 加 `diverse_wide/town` + 三个静止场景各 5 帧 | 路缘位姿 0.000/0.000 → **0.554/0.411**；城郊引道 0.000/0.000 → **0.575/0.631**；`frozen_wide2` precision 0.265→**0.597**、recall 0.942→0.987、IoU 0.261→0.592 | **支持**（两处退化消失；独立路段 2.3×） | **未在未见地图/街区验证**；同一街走廊、同一会话 |
| `ft_t11` 应接入生产 | **未激活** | — | 静态指标改善，但没有闭环与全链验收 | **未支持**（不接线） | 驾驶层收益与风险 |

**本轮补充的跨地图结果**：`east_coast_usa`（25 帧，训练集无该地图）上 `v13b` 基线 precision 0.044 / recall 0.569 /
IoU 0.043 → `ft_t11` **0.303 / 0.345 / 0.192**（precision 7×、IoU 4.5×，**但跨地图召回从 0.987 掉到 0.345**）。
跨地图**训练**数据采集三次尝试均在 `east_coast_usa` 上中断（单视角环同样中断，固定位姿采集成功）→
登记为模拟器阻断 + 下一步配方（路网引导沿路采样）。

**未测清单（不得当成通过）**：跨地图**训练数据**与跨街区未见场景；夜间/强光/阴影下的线通道；鱼眼域**微调后**的增益
（本轮只把鱼眼帧采进数据，未单独评估）；驾驶层的碰撞/压线/出铺装/停车/推进/deadline；
`ft_t11` 的推理延迟；多 seed 方差。

**下一步（按计划顺序）**：跨地图未见场景测试 → 若仍成立，再做 T08 的对应关系与地图先验 A/B →
最后才谈接线（T07/T08/T09 全链验收 + 单因子闭环对照）。
