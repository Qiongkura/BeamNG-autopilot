# 交接文档（2026-09-13，换平台继续）

> 新平台接手先读本文件。结论以本文为准；不要把灌木出生点实验误判成标线能力实车验收。

## 1. 当前总状态

- 主力分割模型：`logs/m5_seg/seg_model/best.pt`，当前是从零多域联合训练的 joint 模型；所有地图共用一个模型，`by_map` 专家目录已删除。
- 人工标签：`logs/labeling/line_grid_labels.jsonl`，约 3026 个唯一帧（追加修正记录共约 3433 行）。
- 标注池：`logs/labeling/grid_round3/`，约 2703 帧；新地图/土路/赛道帧已全部进入池并完成宫格标注。
- 代码已推到 GitHub `main`，按模块压缩历史。最近已推送：RL 横向残差 env、训练入口、测试；之前也推送了标注/采集/联合模型路由等工具。
- 全套 pytest 最近通过过；长时间实车实验不要再用 `| tail`，会吞缓冲和退出码；实时日志用 `python -u`。

## 2. 重要复核结论：实车判决实验无效

影子剧集：`logs/m5_e2e/shadow_fsd_1789298914_20260913_193100.npz`（218 帧）

- `lane_src`: sensor 115 / perception-unavailable 103，表面看有配对。
- 但抽帧显示前视画面几乎全是灌木/树枝，没有可见路面和漆线。
- 根因是路网节点被当出生点，车实际传送进树丛；因此 sensor 结果可能是树枝误配。
- 不能把这段当成“黄线能力实车实锤”；也不能把它当 US 正例继续训练。
- 原始有效 east_coast 影子：`shadow_fsd_1789302749_20260913_204303.npz`（约 1104 帧，另一轮可见机位探测；仍应先按画面核验）。

### 正确下一步
1. 用 `spawn_survey_eco` 的机位图人工选“画面确有路面+漆线”的点；不要直接信 roadnet 节点。
2. 已知较好 east_coast 候选：`(246.44, 877.87)`、heading `-107.62°`；避免 `(-190.5,-74.4)`，它在树丛里。
3. 选定可见漆线机位后再跑 120-600 秒短实验。
4. 只有当 `sensor/P` 连续、车真正驶出并有稳定轨迹后，才讨论配对门控和规划域适应。

## 2b. S0.4 有效判决（2026-09-14，合法机位）

**命令**：`m5_fsd_drive --runtime tech --attach --map east_coast_usa
--teleport 246.44 877.87 -107.62 --allow-unplaced --strict --lane-mode sensor
--seconds 120 --speed 6 --no-e2e --no-bc --no-dqn`

**影子**：`logs/m5_e2e/shadow_fsd_1789361284_20260914_125031.npz`（216 帧 / 119s）

| 量 | 值 | 对比灌木段 |
| --- | --- | --- |
| provenance map | **east_coast_usa** | 正确（S0.2 生效） |
| sensor 配对 | **28.2%**（61/216），最长连续 **21** 帧 | 有真实配对，非树枝 |
| trajectory_ok | **96.3%** | 远好于 28% |
| 位移 | **21.1 m** | 真开出去了 |
| speed p50 / max | 0.33 / 2.57 m/s | 能动但远低于 6 |
| kind | lane_center 160 / shift 28 / arc 20 | 规划在工作 |
| 卡点 | `no drivable path` / `path grazes obstacle` 交替；最长 unavailable 79 帧 | 规划域适应 + 配对闪烁 |

**S0.4 结论**：机位与 provenance **已过关**；联合模型在可见路面上能间歇 `sensor/P`；
**尚未**达到连续放置 + 巡航。下一刀是 **配对闪烁（长 79 帧空档）与障碍/走廊门控**，
不是再改出生点。

**本段影子为正例候选**（勿与灌木负例混淆）；训练前仍建议抽帧人眼确认漆线可见。

## 3. 模型与离线指标

联合模型在 3026 人工标签上的按域存在性 AUC（线掩码面积打分）：

- italy：约 0.976-0.978，召回约 0.95；
- east_coast_usa：约 0.944-0.948，召回约 0.96-0.98；
- west_coast_usa：约 0.936，召回约 0.93；
- utah：约 0.984；
- automation_test_track：约 0.956；
- hirochi_raceway：约 0.828（最弱，红白路缘训练样本少）；
- johnson_valley：约 1.0；Industrial 小样本约 0.87；small_island 只有负样本，AUC 无意义。

像素分割验证：joint 的混合验证口径 mIoU 约 0.77、line IoU 约 0.41；不同轮次掺入伪标签，不能直接横向比较。近区/中区手涂真值帧的线检出为 100%；远区缺有效真值，仍未测量。极淡线 f00158 是共同难例：原始模型有少量 line logits，但整图占比低于存在性阈值。

## 4. 训练资产

- `logs/m5_seg/seg_model_joint/`：从零联合训练（旧 joint，曾作为单模型部署）。
- `logs/m5_seg/seg_model_v7/`：加入植被修正伪标签 v2 的 round-7 模型，分域结果：east 0.944、west 0.897、utah 0.966、italy 0.976；尚未部署为线上 best。
- `logs/m5_seg/pseudo_us_map_v2/`：约 709 帧，黄色线 HSV 并集模型线，且剔除强绿植被路面误标。
- `logs/m5_seg/dirt_road_labeled/`：45 帧手涂 road/line；土路 road IoU 现役约 0.72，说明“完全识别不出土路”此前判断过重。
- `beamng_autopilot/vision/yellow_line_mask.py`：HSV 黄线候选。
- `beamng_autopilot/vision/veg_mask.py`：强绿植被候选，用于修正 road 伪标签。

## 5. 横向残差 RL 已完成离线第一步

文件：

- `beamng_autopilot/rl/lateral_env.py`
- `scripts/m5_train_lateral_dqn.py`
- `tests/test_lateral_env.py`

离线程序化道路上 40k 步 DQN：平均横向误差 0.076m，零残差基线 0.128m；奖励 303.7 > 299.8；越界率 0。动作是有界横向残差 {-0.5,-0.25,0,+0.25,+0.5}m，自回中，不能替代基控。`mode="sim"` 仍是 `NotImplementedError`，下一步可接 live FSDStack，但必须先解决有效机位和规划门控。

## 6. 配对滞回 A/B（中断，未下结论）

代码改动：`beamng_autopilot/lane/constants.py` 增加环境变量覆盖：

```text
BEAMNG_LANE_HOLD_NONE_FRAMES
```

默认仍为 4。原因：US 实车日志中 unavailable 间隙可能 5-17 帧。尝试过 HOLD=16，但实验中断，没有可比的完整收尾统计；不要直接固化 16。

重跑命令：

```powershell
$env:BEAMNG_LANE_HOLD_NONE_FRAMES="16"
.venv\Scripts\python.exe -u scripts\m5_fsd_drive.py `
  --runtime tech --map east_coast_usa `
  --teleport 246.44 877.87 -107.62 `
  --allow-unplaced --strict --lane-mode sensor `
  --seconds 120 --speed 6
```

比较日志里的：`lane=sensor/P` 连续段、`perception-unavailable` 连续段、`src=fsd`、速度、stops。短实验先用 120 秒，避免 10 分钟浪费。

融合层本来已有滞回：`beamng_autopilot/lane/fusion.py::choose_sensor_lane`，包含来源切换保持和短 gap 保持；真正要继续查的是 `pair_lane_markings` 的 US 双黄线/白边配对是否抖动，而不是盲目继续放大 hold。

## 7. 下一阶段优先级

1. **先修/验证有效出生点**：这是当前最关键，不要再在树丛机位上调规划。
2. **修 shadow provenance map 写死**：当前 `fsd_drive.py` 影子记录里曾写死 `"map": "italy"`；`data_contract.py` 默认也有 italy 口径。多地图实验前应修正并补测试。
3. 有效机位短实验通过后，再看 `path grazes obstacle` / `no drivable path`：检查 `bev/drivable`、护栏误灌、corridor_free_band 和 route A* 连通性。
4. `m5_map_photo_tour.py` 最近增加了 `stops.json` 坐标存档，但最后一次小 survey 曾在收尾前被强杀，`spawn_survey_eco` 可能只有帧没有 stops.json；可按 seed=5/min-dist=200 重放 roadnet 得坐标。
5. hirochi 路缘补涂、远区线真值、sim RL 接入，放在有效实车机位之后。

## 8. 常用命令

```powershell
# 纯逻辑回归
.venv\Scripts\python.exe -m pytest tests/ -q

# 离线深度回归
.venv\Scripts\python.exe scripts/m5_offline_validate.py

# 评估某模型
.venv\Scripts\python.exe scripts/m5_line_presence_eval.py --model <ckpt>

# 新地图传送采集（机位会增量落盘；卡死时 taskkill 游戏，不丢已存帧）
.venv\Scripts\python.exe scripts/m5_map_photo_tour.py --runtime tech --launch --map <map> --stops 250

# 宫格标注
.venv\Scripts\python.exe scripts/m5_line_grid_labeler.py --src <dir> --strategy score --scores <json> --port 8790

# 横向残差 DQN
.venv\Scripts\python.exe scripts/m5_train_lateral_dqn.py --steps 40000
```

## 9. 提交/平台状态

- 当前主线是 `main`；main 历史已按模块重写，远端 GitHub main 已更新。
- `AGENTS.md` 第 6 条明确要求按模块提交，禁止把多个模块攒成细碎提交。
- 换平台前先 `git status`；本轮 `lane/constants.py` 的 env hold 改动、`m5_map_photo_tour.py` 的 stops 存档改动如果尚未提交，应先拆模块提交后再推送。
