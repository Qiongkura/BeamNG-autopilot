# T01–T16 独立复核报告（方案 v2 §5）

复核人：子 agent F（独立复核者，非实现者）
复核时间：2026-09-26 21:20–21:50（本地）
复核对象：工作区 `I:/projects/beamng-autopilot`，HEAD `2ad703a`（21:11 提交，v5 三个提交）+ **未提交工作区改动**
复核方式：只读代码、只读产物、in-process 复现；**未跑 pytest、未跑训练/采集/计时**。
复核快照哈希（复核期间文件仍在被并发修改，见 §复核方法 4）：

| 文件 | sha256（复核时） |
|---|---|
| `beamng_autopilot/experiments/manifest.py` | `2b1df29cc67af60a41ba34fb99719d798dc24f075d1209bf107aadcdde94b3f1` |
| `beamng_autopilot/experiments/candidate_metrics.py` | `dde81891dd581bb7fc58aee4386861b66e8b02b942d8657a0dc9a61d066af2a8` |
| `beamng_autopilot/experiments/gates.py` | `5841a44ec84b583c4bc56bb299bd28e380d9abaf33c383fdf056ac61ee51167a` |
| `beamng_autopilot/experiments/negative_scenes.py` | `caaf163dad40a25a846aa35be676707463c2c114db16cd4b36f0ffeaf32f44ff`（21:29:54） |
| `beamng_autopilot/experiments/proposer.py` | `c82a2797351a016c7db7cd42fc943a9efd3a6b9e960d453af665b7f0a07f3044`（21:32:06） |
| `scripts/m5_seg_autoloop.py` | `7961e7c645b75983494f5ea4e40e550c9271f81ab5e6535a4cd8905d8c6284a9`（21:36:54） |
| `scripts/m5_marking_identity_probe.py` | `b02bcf04b8ed3f38cfc0c967901462b84a2b8c5842a0ba5ae0089abd06bb20e8` |
| `scripts/m5_identity_calibration.py` | `9995f37e675fdaf97e23fa6a4e7257ec4d01c25c74b3e5fdf2873e8004e361e4` |

本报告不采信 `docs/T14_D1_EVAL_CHAIN_REPORT_20260926.md` §6 的自评表；下表每条结论都给出可定位证据，
并区分"我看到了证据"与"实现者声称"。

---

## 一、结论摘要

**通过（4 条）**：T03、T04（均在库/探针单元层）、T08、T13（本轮相关部分）。
**部分（10 条）**：T01、T05、T06、T07、T09、T10、T11、T12、T14、T15（其中 T06/T07 是**接线失效**，不是"未接线"）。
**未测/未实现（2 条）**：T02（且现行为比"未测"更差，见下）、T16。

### 最严重的 3 个缺口

1. **T06/T07 的 30 条逐场景下限在真实 `rounds` 路径上永不触发，并把每个场景标成 `not_applicable`。**
   `scripts/m5_seg_autoloop.py:2167-2169` 用 `_idc["per_group"]`（键为 7 个**比率**名 + `n_candidates`）
   填 `scene_ident_by_seed`，`:2270-2278` 却按 `P_frames/C/R/M/L/A` 逐键读它 → 全部取到 0；
   `gates.scene_count_violations` 于是给 `low_sample=[]`，且因 `P_frames=0` 把所有场景判成
   `not_applicable`（"确认真无线"，`gates.py:506-512` 还会把它从 missing/low_sample 里过滤掉）。
   我用真实的 `identity_metrics` + 假探针（P=3,C=100,R=62,M=9）复现：判定侧拿到的是全 0，
   30 下限不响；正确计数其实就在同一次返回的 `counts_by_group` 里，但只被写进判定文件
   （`:2398`）而**不参与判定**，`scene_counts`（`:2399`）落盘的是全 0。
   这与 D1 报告"R=29→low_sample、R=30→通过 …… 接入 rounds 判定"直接矛盾：库能过测试，
   真实消费路径是死的。

2. **T01/T02 的去重语义不满足方案 §3.1：只按键内容哈希，不检标签冲突、不检几何身份，保留哪一份随输入顺序。**
   `manifest.py:389-401` 只比较 `content_sha16`；全库无冲突检测（`label_sha16` 只进
   `_dataset_id` 与记录字段）。我在临时目录实测：**同 RGB、不同标签**两份 →
   第二份被写成 "byte-identical to …" 拒绝，保留条随目录顺序翻转，`audit()` 里没有任何
   conflict 字段——正是方案禁止的"因路径排序静默覆盖权威标签"（只是覆盖方向是"保留先扫到的"）。
   来源别名也没有结构化保留：标定脚本 `m5_identity_calibration.py:67-76` 的
   `content_aliases` 用 **label_sha16** 且只遍历**已接受**帧，产品
   `identity_calibration_v3.json` 里 `content_alias_groups = 0`（23 个真实别名一个都没报）；
   被拒帧 reason 里的 "in … front_main" 因 `run=rd.name` 恒为 `front_main`，无法定位来源包。

3. **T12/T05 "同一套实现、同一套分母、同时报告 micro/macro"不成立。**
   标定脚本 `m5_identity_calibration.py:191-232` **另抄一套** R/C、M/R、A/L 公式（全文不 import
   `candidate_metrics`）；`cmd_evaluate`（`autoloop:250-278`）根本不计算任何计数/比率，只读外部
   `pairings_*.json` / `hard_gate_*.json`，而全库**没有任何脚本生成这两个文件**（`controller.py:616-625`
   只传路径）；`cmd_replay`（`:361-391`）用存档比率重跑 `decide`，不从计数重算。
   产物侧：75 个 `decision_*.json` 里 **0 个**带 `counts/counts_by_group/scene_counts`
   （最新判定 17:40，早于 21:11 的 v5 提交），因此"不出现 0.80 与 0.40 两套结果"没有任何
   端到端证据；`candidate_metrics.micro_macro` 除测试与协议文本外**无任何调用方**，
   macro 从未在任何入口/产物里报告过。

---

## 二、T01–T16 逐条复核

| ID | 状态 | 证据（我实际看到的） | 缺口 |
|---|---|---|---|
| T01 | **部分** | `manifest.py:389-401` 内容哈希去重（保留扫描首条，其余置 `reject_reason`）；`tests/test_experiments.py:110-126` 断言复制帧被隔离（4 帧）；产品 `logs/experiments/review_pack_20260926/identity_calibration_v3.json`：`inventory={raw_inputs:159, n_unique:136, n_rejected:23}`，23 条 reason 全为 byte-identical | ①别名未保留：产品 `content_alias_groups=0`、`content_aliases={}`（`m5_identity_calibration.py:67-76` 口径本身写错：按 label_sha16 且只统计已接受帧）；②被拒理由的 "in front_main" 定位不到来源包；③"顺序变化指标不变"只在同标签复制时成立：同内容不同标签时保留条与 `dataset_id` 随目录顺序变（本轮实测）；④无任何产物重现"22 个重复内容组"（`grep -rl n_duplicate_groups` 在 `logs/experiments/t14_s0_20260926`、`review_pack_20260926` 为空） |
| T02 | **未实现/未测**（自评"部分"，实际更差） | `manifest.py:389-401` 只比 `content_sha16`；`audit()` 只有 group/content/exposure overlap，无 conflict 字段；无测试。本轮实测（临时目录、两次 build）：同 RGB 不同标签 → 第二条判 byte-identical，`mf.by_split("train")[0].label_sha16` 随 `[a,b]` / `[b,a]` 翻转，`dataset_id` 也变 | 方案 §3.1"同 RGB 标签不同：记录冲突 …… 不能因路径排序静默覆盖权威标签"**未实现**：冲突未检出、未留痕，被当重复静默丢弃；"尺寸/相机/位姿身份不一致先隔离"同样只靠内容相同顺带拒绝（理由错误、无身份比较） |
| T03 | **通过（库/单元层）** | `candidate_metrics.py:30-74`（COUNTERS、R/C、M/R、A/L、零分母→None+原因）；`tests/test_candidate_metrics.py:24-41`（0.80/0.75，C_outside_P=10）；探针把非 P 帧候选记 `C_outside_P`：`m5_marking_identity_probe.py:697-717`；`tests/test_marking_identity_probe_counts.py:245-275` | 无 v5 端到端产物（见 T12）；`summary["counts"]["P_frames"]` 被覆盖成"有引擎线**像素**的帧数"（`probe:805,831,853-854`），而逐帧 C/R 门槛用的是"引擎线**能链成线**"（`probe:697`）→ 报告的 P_frames ≥ 真正进 C 的帧数；标定又把它当 `frames_with_line_ref`（`m5_identity_calibration.py:182-184`） |
| T04 | **通过（探针层）** | `probe:697` `_p_frame = bool(eng_lines)` 逐帧判定；`tests/test_marking_identity_probe_counts.py:245-275`：同一目录两帧（一帧有线、一帧全零）→ `rows[1]` 的 `P_frames=0`、`C_outside_P=1`；标定按目录逐帧累加（`m5_identity_calibration.py:149-158`） | 与 T03 同一个 P_frames 汇总口径问题；P 的"真值"取自 npz 的 label 通道（复核集里就是人工真值，命名沿用 engine，读代码确认无歧义） |
| T05 | **部分** | `candidate_metrics.py:155-181`：micro=整数求和后算一次；macro 用**未舍入**的逐场景比率再舍一次；`macro_note` 明示 "one unit per scene"；`tests/test_candidate_metrics.py:44-54`、`tests/test_experiments_gates_v5.py:165-177` | macro **无任何生产消费方**（grep `micro_macro` 只命中 tests/protocol 文本）；D1 §4 实测表与标定 JSON 只报 micro → 方案"同时报告 micro、macro（明确单位）"未接线；T05 的"macro 若展示必须注明"目前只在库文档+测试里成立 |
| T06 | **部分（真实路径失效）** | 库：`candidate_metrics.scene_report:112-152`（下限只卡 R，消息带 C）+ `gates.scene_count_violations:480-527`；`tests/test_candidate_metrics.py:57-73`、`tests/test_experiments_gates_v5.py:45-69`（R=29/30 翻转；未声明时不生效） | **rounds 接线键空间错配**：`autoloop:2167-2169`（读 `per_group`）→ `:2270-2278` → 全 0 → `low_sample=[]`，场景被判 `not_applicable`。本轮 in-process 复现（P=3,C=100,R=62 → 判定输入全 0）。正确计数在 `counts_by_group`（`:2398` 只落盘、不判定） |
| T07 | **部分（同 T06）** | 库：`tests/test_candidate_metrics.py:70-73` 与 `tests/test_experiments_gates_v5.py:72-93`：C=100/R=1 → `insufficient`，消息确实带 `C=100`；`candidate_metrics.py:127-138` 的 `low_sample` 文本模板含 C | 同 T06：真实 rounds 路径永不触发；`evaluate` 路径连逐场景计数都没有（T12） |
| T08 | **通过** | `gates.per_seed_gate_violations:436-448`、`per_seed_missing:451-460`、`scene_report:463-477`；`tests/test_experiments.py:808-831`（坏 seed：均值看不出、逐 seed 违反→rejected，理由含 seed id 与指标名）、`:834-864`（坏场景/缺测场景两通道）；rounds 汇入 `_missing`/`_violations`：`autoloop:2303-2312` | ①`decide()` 在 `hard_gate_violations` 非空时只回"违反+配对结论"，`decision.reasons` 不再列缺测项（`blob.missing_metrics` 仍留档）——同时存在的两类问题在 reasons 里只留一类；②`hard_by_seed` 只存比率不存逐 seed 整数计数 → 逐 seed 无法按新分母单独重判（只有总体 `counts_by_group` 可追溯） |
| T09 | **部分** | 适用性三态：`candidate_metrics.applicability:83-109` + `gates.scene_count_violations:513-527`；`tests/test_experiments_gates_v5.py:96-138`；"真无线+空预测=实测零误报"`negative_scenes.py:44-54` + `tests/test_negative_scenes.py:24-31`；"有线+空预测 recall=0"：`m5_seg_eval_matrix.py:169-172`（tp+fn>0 → 0.0） | **缺相机不是逐帧 UNKNOWN**：`probe:625` `if cam is not None:` 没有 else —— 无相机的帧不产生 `counts`、不进 `errors`、仍计入 `frames_processed`，还能经 `n_engine_px` 计入 `counts["P_frames"]`/`frames_with_engine_line`；只有 run 级 `camera_model_used`（`probe:896`）可见。方案"缺相机 → UNKNOWN"只做到 run 粒度 |
| T10 | **部分（自评"未测"已过时）** | 工作区（未提交，mtime 21:29:54）：`negative_scenes.py:20-55` 档位门槛（`label_rank != verified` → `unverified_frames`+`unverified_pred_line_px`，不进合格分母；`None` 也按不可信）、`:58-85` 排除量与原因；接线 `m5_seg_eval_matrix.py:36-45`（读**标注凭证**取档位）、`:225-243`（`load_frames(with_rank=True)` 带档位）、`:87-91`；测试 `tests/test_negative_scenes.py::test_t10_a_non_verified_label_cannot_be_a_clean_negative`、`tests/test_seg_tooling.py::TestEvalMatrixMath::test_negative_scene_wiring_and_explicit_device`（21:30 改） | ①未进判定文件/看板（grep 无 `negative_line` 消费方）→ §3.5"先作为版本化诊断接入判定文件/看板"未完成；②档位按**目录**凭证取，同一目录内混合来源无法分开计数（"混来源分开计数"只在调用方按目录分开时成立）；③本轮只读代码与测试，未跑端到端 |
| T11 | **部分** | 探针失败可见：`probe:577-593`（缺 label 整体拒绝）、`:759-800`（逐帧异常结构化入 `errors`、不进 rows、全失败返回 reason）、`:859-866`（`frames_processed/frames_skipped/skipped_frames/n_errors`）；`tests/test_marking_identity_probe_counts.py:230-239/281-308/310-323/343-355/381-403`；缺 `counts` → 比率 None：`autoloop:1237-1242` + `tests/test_seg_rounds_wiring.py:1256-1262` | ①`identity_metrics` 仍静默丢整个 run：meta 缺失 `continue`（`:1227-1228`）、探针抛异常 `except: continue`（`:1232-1233`），无 skipped 计数/原因输出——正是方案 §S2 禁止的写法；②缺 `counts` 时落盘 `counts` 是**全 0 字典**（测试断言 `C==0`），与"实测 0"不可区分，而 `gates._is_count_blob:563-572` 把 0 当合法读数 → 全 0 会被判"带 v5 计数、可重放"；③标定消费 `frames_processed or frames or 0`（`m5_identity_calibration.py:166`），两者都缺时静默加 0、不报 UNKNOWN（违背 §S1.3） |
| T12 | **部分** | rounds 唯一实现：`autoloop:1207-1276`（`_cm.empty/accumulate/ratios`）；gates 直接复用：`gates.py:500-502`；测试 `tests/test_experiments_gates_v5.py:141-177`（gates 条目与 `cm.ratios` 逐一相同、micro≠macro） | ①标定另抄公式（`m5_identity_calibration.py:191-232`，不 import `candidate_metrics`）；②`cmd_evaluate` 不算计数（`autoloop:250-278`），输入是外部文件，且全库无生成器（`controller.py:616-625` 只传路径）→ evaluate 的分母来源不可自动保证；③`cmd_replay` 只用存档比率重跑（`:361-391`）；④产物：75/75 判定无 v5 计数，v5 后无判定产物 → 跨入口一致性无端到端证据（自评"通过（门控测试）"只证明了 gates↔candidate_metrics 一层） |
| T13 | **通过（相关部分）** | 旧记录：`gates.LEGACY_REPLAY_NOTE:550-554`、`legacy_replay_note:615-…`、`_is_count_blob:563-572`；`cmd_replay:365-368` 跳过重判并打印说明、`:401-405` 统计；`tests/test_experiments_gates_v5.py:180-233`（只读、不补零）；最终集：`final_set.py:191-225`（未封存/协议不符/已消费/用途不符全部拒 + 追加账本），产品 `logs/experiments/t14_w0_20260925/smoke_final/seal/final_set_ledger.jsonl`（1 次 allowed → "already consumed" 拒 → purpose=train 拒）；测试 `tests/test_seg_rounds_wiring.py:790-832`（第二次确认 rc=3 + "already consumed"）、`:835-866` | ①"有无新计数"的判据被 T11② 削弱（缺测被物化成全 0 → 仍判可重放）；②"协议/权重漂移"在 evaluate 侧靠人工 `--definition-drift`（`autoloop:2610`），不是自动检出 |
| T14 | **部分（自评"未测"，实际已在建）** | 工作区（未提交）：`proposer.factor_activity`（`proposer.py:150-195`，`LINE_LOSS_KEYS` 表）+ `tests/test_experiments_proposer_active.py:39/59/163`（road-only 下线损失键 inactive、propose 不产出该提议）；rounds 拒绝训练：`autoloop:1929-1945`（inactive → `factor_not_applied` → return 3，训练命令根本不会装配） | ①缺"road-only + 线损失提议走 rounds → 真实训练调用 0 次"的端到端用例（现有 rounds 反例是 `group_weights`/空因子）；②S5 其余交付（提议/实际参数/实际损失项/生效步数台账、清理与恢复校验）未见 |
| T15 | **部分（已有既有测试覆盖，非本轮用例）** | `tests/test_seg_losses.py:48-60`：同一 20px 重叠下 FN=21 与 FP=21，显式 alpha=0.3/beta=0.7 → `l_miss > l_fp`；`:185-198`：未知区里改预测不影响损失；`:98-123`：屏蔽类梯度恒 0；`:76-86`：255 边界忽略后仍可反传 | 测试是**既有**的（本轮未改 `test_seg_losses.py`），未按 T15 命名、未作为方案 v2 验收反例；只测一对 (alpha,beta)，未覆盖方案点名的"整体 Tversky 权重 ≠ FP/FN 相对权重"混淆；自评"未测（S6）"低估了现有覆盖 |
| T16 | **未测** | `grep` `scripts/m5_seg_dashboard.py`：无 `scene_counts` / `counts_by_group` / `C_outside_P` / `not_applicable` / `unverified` / `excluded_frames` / `negative_line`；看板仍用旧 `match_rate` 填 `candidate_identity_rate`（`:3351-3365`） | 属 S4，未开始；本轮无页面检查记录；判定→看板的 v5 计数/适用性/负例诊断全部缺失（依赖 S3 判定产物先修好，尤其 T06/T07） |

---

## 三、与 D1 报告 §6 自评的差异（复核价值所在）

| D1 报告自评 | 复核事实 |
|---|---|
| T01 **通过**（"S1 审计清单；拒绝 23 帧带理由"） | 去重计数成立，但**别名保留为 0**：`content_alias_groups=0`（口径写错 + 只统计已接受帧），且"顺序变化不变"在同内容不同标签时不成立 → 只能算**部分** |
| T02 **部分**（"内容重复隔离 ✓；同内容不同标签的冲突检测在 `DatasetManifest` 已有"） | 该冲突检测**不存在**：`manifest.py:389-401` 只比内容哈希，全库无冲突字段/留痕。实测保留条随顺序翻转 → 记为**未实现/未测**，且行为违反 §3.1 原文 |
| T05 **通过**（"micro 0.20 vs macro 0.5556，macro 注明单位"） | 数值对，但 macro **没有任何入口报告**，等于只在库里存在 → **部分** |
| T06/T07 **通过**（"接入 rounds 判定：R=29→low_sample、C=100/R=1 仍不足"） | 库测试通过，但 rounds 实际喂给门的是**全 0**（`per_group` 键空间错配），门不响且场景被标 `not_applicable` → **部分（接线失效）**，是本次最严重缺口 |
| T08 **通过**（"上轮逐 seed/分场景硬门 + 本轮场景计数"） | 逐 seed/分场景硬门确实在且已接线（通过）；但"本轮场景计数"恰是坏的那部分，`scene_counts` 落盘全 0 → 该句不成立 |
| T10 **未测**（"astra 的 label_rank 建议仍未落"） | 复核时工作区（21:29:54，未提交）rank 门槛**已落地**并有专测 → 该自评**已过时**；剩余缺口是判定/看板接线 |
| T15 **未测**（S6） | 方案 T15 的两项要求（FP/FN 方向、ignore 不参与监督）已被既有 `test_seg_losses.py` 覆盖大部 → 应记**部分（既有覆盖）** |
| §2 S1 "标定 JSON 记 … 与别名" | 产品里 `content_alias_groups: 0`、`content_aliases: {}` —— "别名"字段事实上没有产出 |
| §4 实测表（C=363/R=353 等） | 与 `identity_calibration_v3.json` 完全一致（可核对）；但该文件时间 20:55，早于 21:11 的 v5 提交，是"提交前工作区"的产物 |
| §5.2 "拒绝 23（全部为内容重复，带理由）"；回归 2877 passed | 均核实为真：23 条 reason 全为 byte-identical；`logs/experiments/gate_20260926_210354.log` 尾部 `2877 passed in 383.35s`——**但该日志 21:10 生成，早于 21:29–21:36 的并发改动**（negative_scenes/eval_matrix/proposer/autoloop），不能覆盖当前工作区 |

---

## 四、复核窗口内出现的并发交付（**未纳入**上表判定）

本次复核期间（21:40–21:49）工作区新增了其它子 agent 的交付，**晚于**我的证据快照，
我只登记、不背书（未运行、未逐行复核）：

| 新文件/改动（mtime） | 与本报告的关系 |
|---|---|
| `tests/test_s5_factor_activity_wiring.py`（21:40）、`proposer.py`（21:32，modified） | 其中 `:87 test_a_line_loss_factor_is_refused_when_line_supervision_is_masked` 正是我在 T14 指出的"缺失的 road-only+线损失 rounds 端到端反例"（断言 rc=3、无 `round0`、无 training-done 事件）。若该测试通过，T14 可从"部分"上修为"通过"——需主 agent 实际运行确认 |
| `tests/test_seg_losses_direction.py`（21:44）、`docs/T15_SEG_LOSS_DIRECTION_20260926.md`（21:47） | T15 的专测与记录，可能覆盖我"只测一对 (alpha,beta)"的缺口；未复核 |
| `tests/test_dashboard_v5_panels.py`（21:49）、`m5_seg_dashboard.py`（modified） | T16/S4 的看板面板测试，可能覆盖"看板无 v5 字段"；未复核（页面实际渲染未检查） |
| `beamng_autopilot/experiments/controller.py`（modified） | 未复核 |

**关键：两个最严重缺口在我最后一次哈希时仍未被这次并发改动触及**——
`scripts/m5_seg_autoloop.py` 的 sha256 仍为 `7961e7c6…`（21:36:54 后未变），
`beamng_autopilot/experiments/manifest.py` 仍为 `2b1df29c…`（15:14:24 后未变），
所以 **T06/T07 的 `per_group` 键空间错配**与 **T01/T02 的冲突/别名语义**在复核结束时依然存在。

## 五、复核方法（可复现）

### 1. 读过的文件（均为只读）

- 方案与报告：`docs/T14_DEVELOPMENT_EXECUTION_PLAN_V2_20260926.md`（§3.1/§3.3/§3.4/§3.5/§5）、`docs/T14_D1_EVAL_CHAIN_REPORT_20260926.md`、`docs/metric_protocol_v5.json`、`docs/t14_thresholds_v4.json`（经测试引用核对）
- 库：`beamng_autopilot/experiments/{manifest,candidate_metrics,gates,negative_scenes,final_set,protocol,credentials,proposer,lineage,spatial}.py`
- 脚本：`scripts/{m5_marking_identity_probe,m5_identity_calibration,m5_seg_autoloop,m5_seg_eval_matrix,m5_final_set,m5_seg_dataset_audit,m5_seg_dashboard}.py`
- 测试：`tests/{test_candidate_metrics,test_experiments_gates_v5,test_marking_identity_probe_counts,test_experiments,test_seg_rounds_wiring,test_seg_losses,test_negative_scenes,test_seg_tooling,test_experiments_final_set}.py`
- 产物：`logs/experiments/review_pack_20260926/{identity_calibration,identity_calibration_v2,identity_calibration_v3,review_audit}.json`、`logs/experiments/t14_s0_20260926/{baseline_freeze,lineage_audit}.json`、`logs/experiments/t14_w0_20260925/smoke_final/seal/{final_set_seal.json,final_set_ledger.jsonl}`、`logs/experiments/gate_20260926_210354.log`、75 个 `logs/experiments/**/decision_*.json`（脚本汇总扫描）

### 2. 跑过的命令（全部秒级、无 GPU/无训练/无采集）

- `git log/status/diff`：确认 HEAD `2ad703a`、v5 三个提交时间 21:11、工作区 4 改 + 4 未跟踪；`git diff scripts/m5_seg_eval_matrix.py`、`git diff scripts/dev_validate.ps1`
- 定点 `grep`：`candidate_metrics` 的引用方（确认标定脚本**不在**名单内）、`frames_with_engine_line`/`P_frames`/`scene_ident_by_seed`/`counts_by_group`、`pairings_`、`negative_line`、`micro_macro`、看板字段
- 协议自查：`snapshot_hash` 重算 `metric_protocol_v5.json` → `275cddb5834ba549` 与记录一致（哈希自洽）
- **T06/T07 失效复现**（in-process，假探针，无模型）：
  用 `importlib` 载入 `scripts/m5_seg_autoloop.py` → `identity_metrics(Path("ck.pt"), [真实复核目录], probe_fn=fake)`，
  其中 `fake` 返回 `counts={"P_frames":3,"C":100,"R":62,"M":9,"L":8,"A":4}`；
  打印 `per_group` 键（7 个比率/n_candidates 名，无 C/R）→ 按 `autoloop:2270-2278` 的循环构造 `_scene_counts` → 全 0 →
  `gates.scene_count_violations(_scene_counts, Thresholds(per_scene_min_candidates=30))` → `low_sample=[]`、
  `missing=[]`、`applicability="not_applicable"`。
- **T01/T02 语义复现**（`tempfile` 临时目录，不写仓库）：两份 `front_main`，RGB 逐字节相同、label 不同，
  `DatasetManifest.build([a,b])` 与 `([b,a])` 两次 → 保留条与 `dataset_id` 随顺序变，无冲突字段。
- **T10 档位实读**：对 `review_pack_20260926/reviewed/*/front_main` 调
  `read_dir_credentials` + `m5_seg_eval_matrix._frame_rank` → 8 个目录全部 `human_revision` → `verified`
  （证明档位门槛在真实复核集上不会误伤，同时说明它确实需要凭证）。
- 刻意**未**执行：`pytest`（任何文件）、训练、采集、计时、`content_digest_index` 全量哈希扫描
  （159 帧磁盘读取在并发负载下超时，已终止；159/136/23 取自交付产物，未独立重算）。

### 3. 判定规则

- "通过"= 我在代码+测试/产物里找到了直接证据，且该证据覆盖 T 项要求；
- "部分"= 库/单元层成立但消费路径缺失或口径有缺口；
- "未测"= 找不到证据（不因实现者声称而记为通过）；
- "未实现"= 代码里确认不存在该能力。

### 4. 复核快照与并发修改声明

复核期间（21:29–21:36）本仓库有**并发写入**：`negative_scenes.py`、`m5_seg_eval_matrix.py`、
`test_negative_scenes.py`（21:29–21:30）、`proposer.py`、`test_experiments_proposer_active.py`（21:32）、
`m5_seg_autoloop.py`（21:36:54，我在复核中途重新读取并重新记录哈希）。
上表所有 `path:line` 以 §header 哈希为准；若实现者继续改动，T06/T07 的 `per_group` 错配、
T01/T02 的冲突语义、T10 的判定/看板接线需要重新核对。**本报告只写了这一个新文件，
未改任何代码/测试/配置/既有文档，未 git commit。

---
---

# 第二部分：F2 独立复核（第二复核者，执行型）

> **写作冲突声明**：本节作者是**子 agent F2**。派单指定的唯一写入路径就是本文件，但落笔时
> 发现该路径已被**子 agent F（F1）** 于 21:52 完成的静态复核占用。按项目纪律"复核者不覆盖他人文件"，
> **上方 F1 报告原文一字未动**，本节以追加方式独立记录 F2 的结果。两部分是**两次独立复核**，
> 结论逐条给出，重叠之处可互相对照（F1 未运行 pytest，F2 的增量是**实际执行**）。
> F2 与实现者不是同一 agent：本节所有测试、复现脚本均为 F2 独立编写/运行，未改任何源码/测试/配置/日志。

- 复核时间：2026-09-26 21:35–22:10（本地）
- 复核准入：`HEAD 2ad703a` + 工作区未提交改动（多 agent 并发写入中）
- 纪律：仅跑**定向** pytest（逐条串行，不跑全回归）；无训练、无游戏、无 GPU；反例脚本写在 `%TEMP%`，不写仓库。

## 0. F2 复核快照（并发写入期间，逐次以哈希固定）

复核窗口内 `scripts/m5_seg_autoloop.py`、`scripts/m5_seg_dashboard.py`、`scripts/m5_seg_eval_matrix.py`
被其它 agent 连续修改（21:36→22:07 至少 4 次）。**结论按哈希归属**，下表是各次运行时的版本。

| 文件 | sha256（F2 复核时） | 备注 |
|---|---|---|
| `beamng_autopilot/experiments/candidate_metrics.py` | `dde81891…af2a8` | 全程未变 |
| `beamng_autopilot/experiments/gates.py` | `5841a44e…167a` | 全程未变 |
| `beamng_autopilot/experiments/negative_scenes.py` | `caaf163d…44ff` | 全程未变 |
| `beamng_autopilot/experiments/proposer.py` | `c82a2797…3044` | 全程未变 |
| `scripts/m5_identity_calibration.py` | `9995f37e…61e4` | 全程未变 |
| `scripts/m5_seg_autoloop.py`（早期） | `7961e7c6…a4a9`（21:36） | T06/T07 接线失效版本 |
| `scripts/m5_seg_autoloop.py`（最终） | `aba57e19…f917`（22:04 后） | T06/T07 接线**已修**（见 §3 C3） |
| `scripts/m5_seg_dashboard.py`（最终） | `d4f7f05e…c2e3`（22:03） | T16 面板测试全绿 |
| `tests/test_*`（运行时的测试文件哈希） | 见 §2 每条命令 | 测试文件也在被并发修改 |

## 1. 方法与命令

1. 通读方案 §5 表 + §3.3/§3.4/§3.5/§S1–S4；把 T01–T16 映射到测试函数（grep 全 `tests/`，含任务书点名的 10 个文件及其它相关文件）。
2. 对每个映射到的测试做**定向串行**运行（`.venv/Scripts/python.exe -m pytest <node/file> -p no:cacheprovider`，输出重定向到 `%TEMP%` 后读取；pytest.ini 自带 `-q`，所以不再叠加 `-q` 以保留汇总行）。
3. 用真实库（`candidate_metrics` / `gates` / `manifest` / `identity_metrics` / 探针）+ 临时目录/假探针构造反例（不落仓库）。
4. 判定规则：`covered` = 有测试且我实际跑绿，测试真的触发目标行为；`partial` = 部分成立或覆盖不到真实消费路径；`missing` = 找不到证据。

## 2. 实际跑过的命令与结果（逐条、串行）

| # | 命令（工作目录=仓库根，前缀 `.venv/Scripts/python.exe -m pytest`，后缀 `-p no:cacheprovider`） | 结果 |
|---|---|---|
| R1 | `tests/test_candidate_metrics.py` | **6 passed**（0.18s） |
| R2 | `tests/test_experiments_gates_v5.py` | **7 passed**（0.26s） |
| R3 | `tests/test_marking_identity_probe_counts.py` | **17 passed**（6.63s） |
| R4 | `tests/test_negative_scenes.py tests/test_experiments_protocol.py` | **14 passed**（4.77s） |
| R5 | `tests/test_experiments_final_set.py` | **8 passed**（0.39s） |
| R6 | `tests/test_seg_rounds_wiring.py::test_identity_metrics_report_coverage_and_roles` `::test_a_round_decision_replays_byte_for_byte` `::test_seed_hard_covers_every_hard_check` | **3 passed**（15.41s）；前两条在最终 autoloop `aba57e19` 上重跑：**2 passed**（4.43s） |
| R7 | `tests/test_experiments.py::test_a_bad_seed_cannot_hide_behind_the_mean` `::test_a_bad_scene_cannot_hide_behind_the_pooled_mean` `::test_byte_identical_frames_are_rejected_not_split` `::test_the_digest_index_finds_copies_by_content_not_by_path` `::test_an_unfrozen_gate_blocks_promotion_even_when_it_is_measured` | **5 passed**（0.39s） |
| R8 | `tests/test_seg_losses.py` | **10 passed**（22.0s） |
| R9 | `tests/test_seg_losses_direction.py`（复核窗口新增） | **15 passed**（122.6s） |
| R10 | `tests/test_experiments_proposer_active.py tests/test_s5_factor_activity_wiring.py` | 21:47 版：**16 passed**（23.7s）；最终版本重跑：**13 passed + 1 failed**（24.1s）；失败测试单独重跑：**1 failed**（15.5s）——见 §3 C6 |
| R11 | `tests/test_seg_dashboard.py`（整文件） | **26 passed**，但耗时 **424.2s**；最慢 `test_every_table_header_carries_its_meaning` **420.6s**（`--all-runs` 扫描真实 run 目录）。**注意：单文件超过 60s 预算，仅此一次** |
| R12 | `tests/test_dashboard_v5_panels.py`（复核窗口新增） | 21:50：**1 failed / 6 passed**；21:52 并发编辑中途：6 failed（`NameError: name 'legacy' is not defined`）；22:03 最终版（dashboard `d4f7f05e`）：**8 passed**（2.43s） |
| R13 | `tests/test_annotate_identity.py tests/test_experiments_spatial.py` | **18 passed**（9.91s） |
| R14 | `tests/test_seg_tooling.py::TestEvalMatrixMath`；重跑加 `tests/test_negative_scenes.py` | **8 passed**（8.75s）；**19 passed**（3.20s） |
| R15 | `tests/test_seg_dashboard.py::test_the_decisions_view_shows_per_seed_and_per_scene` `::test_the_decision_view_renders_when_every_hard_gate_input_was_measured`（+ 面板文件） | **4 passed**（3.09s）；合并重跑 **10 passed**（2.31s） |
| R16 | `tests/test_scene_counts_wiring.py`（复核缺口修复后新增） | **3 passed**（2.13s） |

## 3. F2 独立反例（都可复现，脚本在 `%TEMP%/f2_*.py`，不写仓库）

**C1（T01/T12）评价路径不去重：复制目录被计两次。**
构造两个目录，第二份是逐字节复制（内容哈希相同、`source_id` 不同），用真实
`m5_seg_autoloop.identity_metrics` + 假探针（每个目录 C=10）：
`[coll_a, coll_b]` 与 `[coll_b, coll_a]` 都得 `C = 20`、`coverage = 0.8`。
即审计层（`DatasetManifest`）会拒掉复制帧，但**真正算指标的 `identity_metrics` 直接按目录探测**，
不消费审计清单 → 同一份逐帧数据在标定入口是 10 条候选、在 rounds/evaluate 入口是 20 条
（方案 §S2 验收"标定/自动迭代/直接评价原始计数逐项相同"不成立）。最终版 `aba57e19` 仍如此。

**C2（T01/T02）目录顺序改变保留哪一份与 `dataset_id`。**
`DatasetManifest.build([a,b])` 保留 `coll_a`、拒 `coll_b`；`([b,a])` 反过来；
两次 `dataset_id` 不同（`53909d18…` vs `55c43ad8…`）。`FrameRecord` **没有 alias 字段**；
同 RGB、**不同标签**的两份被标成 `byte-identical to frame_00000.npz in front_main`——
隔离确实发生，但**没有"标签冲突"这个类别**，也没有冲突留痕；
同像素但位姿 10 m vs 999 m 的两份同样只按"内容重复"拒绝，**没有任何几何身份比较**。
"不能取最后一份静默通过"字面成立（后到者被拒），但按 §3.1 要求的"记录冲突/按已确认版本选择"**不存在**，
保留条取决于目录顺序（先扫到的胜出）。

**C3（T06/T07）R 下限在真实 rounds 路径曾完全失效，复核窗口内被修复。**
早期版本 `7961e7c6`：`scene_ident_by_seed` 取 `_idc["per_group"]`（键是 7 个比率名），
再按 `P_frames/C/R/M/L/A` 读 → 全 0。用真实 `identity_metrics`（假探针 R=8）+ 复刻该聚合循环：
`rounds scene_counts = {P_frames:0,…,R:0}` → `low_sample=[]`、`missing=[]`、`applicability='not_applicable'`。
最终版 `aba57e19:2174-2177` 改为取 `_idc["counts_by_group"]`（整数），同输入得到
`low_sample=['scene italy/ring_x: R=8 < 30 usable references (C=10)…']`、`applicability='measured'`；
新增 `tests/test_scene_counts_wiring.py` 3 passed 钉住键空间。**该缺口已关闭，但修复只有库层回归测试，
没有"真实 rounds 判定文件里 low_sample 真的出现"的端到端用例**（且 autoloop 在复核期间仍在被改，回归风险高）。

**C4（T11）整目录探针异常仍被静默吞掉。**
`identity_metrics` 对 `meta.json` 不可解析的目录：探针**根本没被调用**，结果 `counts` 全 0、
`coverage=None`，返回里**没有任何 errors/skipped/reason 字段**（`aba57e19:1227-1234` 的
`continue` / `except: continue`）。方案 §S2 "异常不允许被 `except: continue` 静默吞掉"在入口层未做到。
另外，缺 `counts` 时落盘的 `counts` 是**全 0 字典**，而 `gates._is_count_blob` 把 0 当合法读数 →
"没测到"与"测到 0"在 replay 判据里不可区分。

**C5（T09）三态语义本身正确，但两条腿没有测试。**
① 缺相机：真实探针 + 无 `cameras` 的 meta → `camera_model_used=False`、`frames_with_counts=0`，
但 `P_frames` 仍由 label 派发为 1 → `scene_count_violations` 给 `applicability='unknown'`（正确，非 not_applicable）。
② 有线但空预测：`m5_seg_eval_matrix.totals_to_metrics(tp=0,fn=5,gt=5,pred=0)` →
`line_recall=0.0`、`line_precision=None` + "no predicted line pixels at all"（正确）。
两处都没有专项测试（缺相机只有 `camera_from_meta is None` 的单测；recall=0 只有零分母=None 的单测）。

**C6（复核发现的现网红测试，属 S5 归因/看板入口，非 T14 字面项）**
`tests/test_s5_factor_activity_wiring.py::test_a_non_line_factor_still_trains_under_road_only`
在最终版 `aba57e19` + 测试 `431f65c2` 上**失败且可复现**（单独重跑 15.5s）：
`data_counts={'generated':9,'reviewed':0,'trained':0,'evaluated':3}`，断言 `trained > 0` 失败。
根因：`_rounds_audit` 的 `report["coverage"]` 已经是 train split 的覆盖（`aba57e19:1780/1794`），
而 `:2402` 读 `_report["coverage"]["train"]` **多嵌套了一层** → 恒为 `{}` →
`data_counts.trained=0`，并且 `_labels_ok=False` ⇒ 真实 rounds 的 `classify_round_outcome`
会**永远归为 `missing_labels`**，"连续无收益"停止条件永远不会累计（§S5.5 语义失效）。
T14 的核心断言（road-only+线损失 → rc=3、无 round0、无 training-done 事件）仍通过。

## 4. T01–T16 判定（F2 结论；与上方 F1 报告独立）

| ID | F2 判定 | 证据（我实际执行/读到） | 缺什么 |
|---|---|---|---|
| T01 | **partial** | `test_byte_identical_frames_are_rejected_not_split` 绿（复制帧被隔离、不跨集）；标定 `audited_inventory` 走 manifest 去重 | **评价路径不去重**（C1：复制目录 C=20 而唯一图只有 10）；别名无结构化保留（无 alias 字段；`content_aliases` 按 `label_sha16` 且只遍历已接受帧）；顺序会改 `dataset_id` 与保留条（C2） |
| T02 | **partial** | 隔离行为存在：同内容第二份被 `byte-identical` 拒绝并可定位其路径；`test_experiments.py:110`、`test_annotate_identity.py::test_conflicting_identity_kept_and_reported`（仅 sidecar 身份冲突） | **无"标签冲突"检出/留痕/测试**；几何身份不一致不比较（同像素只按重复拒）；保留哪份随目录顺序（C2）；无任何 T02 专项测试 |
| T03 | **covered** | R1,R2：`test_t03_a_reference_scene_and_a_line_free_scene_do_not_mix_denominators`（0.80/0.75、`C_outside_P=10`、无线=not_applicable）；R3 探针互斥计数 | — |
| T04 | **covered** | R3：`test_m_le_r_le_c_and_a_le_l_le_m_and_p_frame_exclusivity` 同目录混合有线/全零帧，逐帧 `P_frame`、非 P 帧候选只进 `C_outside_P` | — |
| T05 | **covered** | R1：micro=2/10=0.20、macro 带 `units`/说明；R2 另断言 micro≠macro；R12 看板面板实际渲染 macro | — |
| T06 | **partial** | R1/R2 库层绿（R=29→insufficient、R=30→ok、未声明不启用）；C3：早期真实路径喂全 0 → 门不响，最终版已修并有 `test_scene_counts_wiring`（R16，3 passed） | 无端到端用例证明"真实 rounds 判定文件里 R<30 进 low_sample"；修复在复核期间才落地，未见全回归确认；逐场景候选覆盖/身份/角色仍**只上报不进任何门**（§S3.4 要求接入场景门控） |
| T07 | **partial** | R1/R2：C=100/R=1 → insufficient 且消息带 C；C3 修复后真实路径同样按 R 判 | 同 T06 的端到端缺口；`evaluate` 子命令本身不计算任何计数（只读外部 pairings 文件） |
| T08 | **covered** | R7：坏 seed/坏场景库层无法被均值抵消，违反→rejected、缺测→needs_evidence；R6 `test_seed_hard_covers_every_hard_check` 防接线漂移；R15 看板逐 seed/逐场景可见 | 残余：`decide()` 在 `hard_gate_violations` 非空时 reasons 不再列缺测项（`blob.missing_metrics` 仍留档）——"多个问题全部原因留存"只做到落盘、没做到单列 |
| T09 | **partial** | R2：无线场景 not_applicable、R=0 UNKNOWN、L=0 UNKNOWN；R4：真无线空预测=measured 零误报；C5 独立验证缺相机→unknown、有线空预测 recall=0.0 | 缺相机的**逐帧** UNKNOWN 无测试（P_frames 仍=1 靠 label 兜底）；recall=0 无专项测试；C4 的全 0 counts 会削弱"缺测 vs 实测 0"区分 |
| T10 | **covered** | R4：`test_t10_a_non_verified_label_cannot_be_a_clean_negative`（agent/未声明均不进合格分母、排除量可见、预测像素只作观测）；R14 `test_negative_scene_wiring_and_explicit_device` 按目录档位排除；R6 `test_declaring_human_revision_cannot_upgrade_agent_data`（CLI 声称 human 不能升格凭证） | 负例诊断尚未进 rounds 判定文件（见 T16） |
| T11 | **partial** | R3：逐帧失败结构化为 errors/skipped、失败帧不进 rows、无掩码帧不算错、装饰失败不丢测量；R6：探针没给 counts → 比率 None | 入口层 `except: continue` 仍静默丢整个 run（C4：无诊断字段）；全 0 counts 会被 `_is_count_blob` 当"实测 0、可重放"；标定 `frames_processed or frames or 0` 两键都缺时静默加 0 |
| T12 | **partial** | R1/R2：`candidate_metrics`↔`gates` 同分母、gates 直接 import 复用；R6：`identity_metrics` 用 `_cm` 计数、无 counts 则 None；R6 replay 逐字一致 | 标定脚本**另抄一套公式**（`m5_identity_calibration.py:185-232`，不 import `candidate_metrics`）；评价路径不去重 → 标定与 rounds 可给出 10 vs 20（C1）；`evaluate` 只读外部文件；final-confirm 明确不跑 C/R/M/L/A（`m5_final_set.py:119-120` 记 UNKNOWN）；无端到端产物证明"0.80 与 0.40"不再并存 |
| T13 | **covered** | R2：legacy blob 只读、不补零、带 v5 计数可重放；R5：封存/协议不符/权重不符/重复访问全部被拒且留痕；R6 replay 对带计数判定逐字一致 | 无"用真实 pre-v5 decision 文件驱动 `replay` CLI"的端到端测试；C4 的全 0 counts 歧义 |
| T14 | **covered** | R9 中 `test_a_line_loss_factor_is_refused_when_line_supervision_is_masked`（road-only+line_weight → rc=3、无 round0、无 training-done 事件、needs_review 留痕）；`test_propose_never_emits_line_loss_proposals_under_road_only`、`factor_activity` 键表对齐 | 残余：C6 的 S5 归因红测试（真实 rounds 归因恒 missing_labels），非 T14 字面项但影响"停止条件只数有意义无收益" |
| T15 | **covered** | R8 既有 10 项（FN 比 FP 贵、ignore 区不参与、屏蔽类梯度 0、255 边界）；R9 新增 15 项：合成 FP/FN 的损失值/切线比=beta/alpha、闭式梯度校验（排除 alpha/beta 对调约定）、`w_tversky` 1→2 是**更偏召回**而非压 FP、真实配方权重 `[0.604,1.0,40.0]` 下方向由 CE 权重主导、ignore 区逐位不变 | —（记录 `m5_train_seg.py` 并未把 `--line-tversky-alpha` 放进 `TRAINER_FLAG_FACTORS`，只有 beta；不影响 T15 方向口径） |
| T16 | **partial** | R12（最终版 8 passed）：七面板渲染、UNKNOWN 不是 0、N/A（确认真无线）与 UNKNOWN 分开、坏场景与池化好数字同屏、pre-v5 判定写"旧协议未记录"、缺字段写"无数据"、原帧可点/不存在写 missing；R15 旧看板判定测试也绿 | 生产者侧缺口：rounds 判定**不写 `negative_line`**（全库只有 eval matrix 写）→ 真实 run 的"无线诊断"面板要等 `--eval`；C6 使"数据入口"面板 `trained=0`、逐轮归因恒 missing_labels；无浏览器/页面实机检查（仅 in-process 渲染断言） |

**F2 计数：covered 8（T03/T04/T05/T08/T10/T13/T14/T15）；partial 8（T01/T02/T06/T07/T09/T11/T12/T16）；missing 0。**

## 5. 任务书点名要查的"门是否真消费新计数"

- `gates.hard_split`：**真消费**。`HARD_CHECKS` 含 `candidate_reference_coverage` / `left_right_role_agreement`
  （`gates.py:383-391`），阈值文件 v4 四个字段全部显式声明（0.80/0.70/30，`config_hash=a02e4db5b46feeaa`），
  `aba57e19:2303` 用本轮 `identity_metrics` 的计数比率进 `hard_split` 与逐 seed 门 → 不是"只在配置里"。
- `identity_metrics`：**真用 `candidate_metrics`**（`_cm.empty/accumulate/ratios`），总体与逐场景计数同源；
  最终版判定文件写 `counts`/`counts_by_group`/`scene_counts`（`:2411-2413`）。
- 但两处**真实缺口**：(a) 评价路径不消费审计清单 → 复制目录双计（C1），与标定不一致；
  (b) 逐场景候选覆盖/身份/角色仍只进 `scene_candidates` 上报，不参与场景门控（§S3.4 原文要求接入）；
  R 下限在 `7961e7c6` 版本完全失效，`aba57e19` 已修（C3）。

## 6. 未测 / 不确定

- 未跑全回归（主 agent 职责）；未跑任何训练/采集/计时/游戏；未做浏览器可视化检查（子 agent 不加载 Browser 技能）。
- `test_seg_dashboard.py` 整文件虽全绿但 **424s**（单测 420s 扫真实 run 目录）——已超出 60s 预算，后续不要作为常规定向测试反复跑。
- 复核窗口内多个文件被并发修改；本报告结论以 §0 哈希为准。`m5_seg_autoloop.py` 在 22:04 后仍可能再变，
  C3/C6 与 §5 的接线结论需在冻结版本上重核。
- 未独立重算 159/136/22 真实数据（不重复 F1 的磁盘扫描；不做 CPU 重活）。
- 本节的唯一写入就是本文件（追加）；未改任何代码/测试/配置/日志，未 commit。**
