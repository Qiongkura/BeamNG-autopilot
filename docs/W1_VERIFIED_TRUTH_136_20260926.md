# 全量人工评价集（136 帧）：verified 真值下的标线测量（2026-09-26）

**这一批是 W1 §6.3 的全量评价集**：136 帧唯一帧、8 个采集组、全部 `verified` 档
（`label_source=human_revision`，复核人 `Administrator`，逐帧 `annotation.reviewer` /
`annotated_at` / `classes_painted` 记在各包 `meta.json`）。
其中 **27 帧带逐像素路型图**（沥青/碎石/路肩；路肩按背景处理）。

**四个计数**（方案 §6.3 验收要求分别可查）：

| 计数 | 值 | 说明 |
| --- | --- | --- |
| 生成帧 | 136 | 全量复核包（`review_pack_full.json`） |
| 人工确认帧 | **136** | 全部 `verified` 档（另有 4 帧与唯一集字节重复，审计按内容隔离） |
| 实际入训帧 | **0** | 这批**还没用于训练**（只作评价真值；避免评价集污染训练） |
| 有效评价帧 | **136** | 本次评估实际用到的帧（见下） |

> 2026-09-26 更正：历史评估函数未向 Segmenter 传递 `device`，且该 JSON 未记录
> 实际设备，因此原文“CPU 推理”未经证实。最新 58 帧负例复测显式使用 CUDA，
> 记录完整权重路径与哈希，见 `NEGATIVE_SCENE_EVAL_DEVELOPMENT_20260926.md`。
> `seed43/best.pt` 与 `seed43/checkpoint_last.pt` 不是同一模型；不能只用 seed 标识权重。
> 8 个采集组也不等于 8 个与训练隔离的组，仍须单独做训练来源与空间隔离审计。

## 1. 结果（同一批 136 帧，历史实际设备未记录）

| 模型 | line_iou | recall | precision | 路外假线比 | road_iou |
| --- | --- | --- | --- | --- | --- |
| 生产模型 `logs/m5_seg/seg_model/best.pt` | 0.2868 | **0.343** | 0.636 | 0.027 | 0.939 |
| 研究候选 `t14_e0_20260925/seed43` | **0.3741** | **0.518** | 0.575 | 0.016 | 0.875 |

逐场景（n = 该场景帧数）：

| 场景 | n | 生产模型 IoU / R / P / 路外 | e0_seed43 IoU / R / P / 路外 |
| --- | --- | --- | --- |
| `135218`（wide） | 6 | 0.502 / 0.649 / 0.690 / 0.023 | 0.354 / 0.483 / 0.570 / 0.0002 |
| `135342`（town） | 24 | 0.367 / 0.425 / 0.728 / 0.012 | 0.459 / 0.477 / **0.924** / 0.0001 |
| `135450`（curve） | 7 | 0.587 / 0.812 / 0.679 / 0.0004 | **0.636 / 0.852** / 0.715 / 0.005 |
| `135717`（plain） | 24 | 0.195 / 0.231 / 0.561 / 0.060 | 0.475 / 0.506 / 0.883 / 0.019 |
| `233648`（junction2） | 9 | **0.026 / 0.034 / 0.106** / 0.090 | 0.257 / 0.299 / 0.643 / **0.261** |
| `233859`（corner） | 8 | 0.070 / 0.076 / 0.455 / 0.020 | 0.575 / 0.606 / 0.917 / 0.0006 |
| `123638`（**砾石土路**） | 28 | IoU / R / P / 路外比均无分母；预测 0 px | IoU 0.0 / R 无分母 / P 0.0 / 0 |
| `123950`（**无标线铺装**） | 30 | IoU / R / P / 路外比均无分母；预测 0 px | IoU / R / P / 路外比均无分母；预测 0 px |

原始 JSON：`logs/experiments/review_pack_20260926/verified_truth_eval_136.json`

## 2. 四条结论

1. **recall 仍是瓶颈，且比 14 帧时更明显**：0.343（生产）/ 0.518（研究候选）；
   两个场景塌得厉害（`233648` 0.034/0.299、`233859` 0.076/0.606）。
   这与 agent 真值下的读数（recall 0.66–0.72）方向完全相反——agent 标注漏标真漆线。
2. **"假线"问题在真值下有了**有证据**的形态**：两个"确认无线"的场景里，
   砾石土路（`123638`）上**研究候选**仍画出标线像素（precision 0.0 = 画的都错），
   **生产模型没有画线**；无标线铺装（`123950`）上这份历史结果的两个模型均无预测。
   零预测是负例上的有效观测，IoU/recall 无分母不等于完全没有测量。也就是说假线不是
   普遍现象，而是**特定材质（砾石/土路）触发**——这比之前"路外假线比 0.5+"的
   agent 真值读数精确得多。
3. **场景差异极大**：line_iou 从 0.026 到 0.636（跨 24 倍），任何池化均值都会
   误导；分场景硬门（§10.2/A7）不是可选项。
4. **两个模型的取舍不同**：研究候选 line_iou/recall 明显更好（0.374/0.518 vs
   0.287/0.343），但 road_iou 更差（0.875 vs 0.939）且路口场景路外假线比更高
   （0.261 vs 0.090）。**不能**据此判"哪个更好"——需要逐 seed 配对与任务硬门。

## 3. 对下一步的直接影响

* **E1（Tversky 复验）方向要改**：~~Tversky 权重 2.0 是压 FP 的~~ —— 该说法
  已被 T15 合成实测推翻（`T15_SEG_LOSS_DIRECTION_20260926.md`）：实现是
  `TP/(TP + alpha*FP + beta*FN)`，只有 `beta/alpha` 比值决定 FP/FN 方向；
  默认 0.7/0.3 时 **FN 更贵**（本就偏向 recall），把整体权重从 1 提到 2 只会
  **更偏 recall**；总损失方向由 CE 类别权重主导（实测 |g_FN|/|g_FP| ≈ 68.7）。
  当前瓶颈是 **FN**，"压 FP"的动机不成立。要真做方向实验应显式测 `beta/alpha`
  比值（alpha 目前不在训练器白名单，需先接线并冻结配方）。
* **E3（困难负例）重新有据**：目标锁定"砾石/土路材质触发假线"这一条，
  而不是泛泛的"路外假线"。
* **门槛标定可以开始**：`paint_valid=136`、`pavement_valid=27`、
  `road_type_frames=27`、8 个采集组。但要注意每场景 6–30 帧、单复核人、
  无标注者一致性证据——标定值必须写清这些前提，且不得为让当前候选过门而调低。

## 4. 局限（不要越读）

* 单复核人、一次通过：**按项目负责人决定（2026-09-26）这批标签即权威真值**，
  不再安排一致性验证；"未做一致性验证"作为事实保留，不得写成"已做"或"一致性好"。
  含义：指标的绝对水平继承复核人的判断口径。
* 两个场景（`123638`/`123950`）**没有标线真值**（人确认无线）：它们的 line 指标
  是"未测"或"有预测但零真值"，**不能**当 0 分或满分读。
* 选帧来自"漆线像素多"的启发式（清晰漆线类）与"确认无线"（另两类），
  不是随机抽样 → 池化 recall 偏乐观，场景内也不代表分布。
* 生产模型与研究候选的训练数据不同，本表**不是**受控对照（对照见 E0/E2）。
