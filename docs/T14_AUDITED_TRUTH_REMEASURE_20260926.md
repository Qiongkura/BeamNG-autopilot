# 审计后真值集重测（S1/S2 验收证据，2026-09-26）

对象：`logs/experiments/review_pack_20260926`（人工复核包）。
目的：回答"136 帧到底是不是 136 张唯一图"，并给出**审计后**的候选口径实测值。
本文件只记录测量，不改阈值；阈值/协议版本见 `docs/t14_thresholds_v4.json`、
`docs/metric_protocol_v5.json`。

## 1. 结论摘要

1. **完成版复核包 136 帧 = 136 张唯一图**，0 拒绝、0 内容冲突（单独审计，见 §2）。
2. **把早先的预览包一起混进来会出现 24 个内容冲突组**：同一张图在预览包里被复制进
   两个槽位、且两次人工标注不同（打包缺陷，已在 `m5_review_pack.py` 的池内去重修掉）。
   预览包是被取代的输入，**不计入测量分母**；冲突逐条留在 `union_audit` 里，不是
   "不扫就没有冲突"。
3. 审计后实测（协议 v5 计数契约、阈值 v4）：两个模型的**覆盖率与左右角色一致率
   过门，身份匹配率不过门**（0.31/0.33 vs 门槛 0.60）——瓶颈在身份/召回，与
   `W1_VERIFIED_TRUTH_136` 的像素结论一致。
4. 逐场景 R 下限（30）在**真实路径**上确实生效：6 个有线场景里 4 个 `R<30`
   （19/31/34/28）标样本不足，2 个（106/148）通过；两个确认无线场景
   （P_frames=0，共 58 帧）记 `not_applicable`，既不通过也不算缺测。

## 2. 清单审计（S1）

命令（可复现；`--trees` 决定测量分母，union 审计固定扫两棵树）：

```pwsh
[Console]::OutputEncoding = [Text.Encoding]::UTF8; .venv/Scripts/python.exe scripts/m5_identity_calibration.py --pack-dir logs/experiments/review_pack_20260926 --model production=logs/m5_seg/seg_model/best.pt --model cand_last=logs/experiments/t14_e0_20260925/seed43/checkpoint_last.pt --device cuda --out logs/experiments/review_pack_20260926/identity_calibration_v5_136.json
```

| 清单 | trees | 原始输入 | 唯一 | 拒绝 | 内容冲突组 | 别名组 |
|---|---|---:|---:|---:|---:|---:|
| **测量清单** | `reviewed_full` | 136 | **136** | 0 | 0 | 0 |
| union 审计 | `reviewed` + `reviewed_full` | 159 | 114 | 45 | **24** | 0 |

冲突根因（逐条可查 `union_audit.conflicts`）：

- 24 个冲突条目 / 22 个内容组，全部**跨两棵树**（预览包 ↔ 完成版）；`kind` 分布
  20 个 `label+identity`、3 个 `label`、1 个 `identity`。
- 预览包 `reviewed/pkg_plain/front_main` 的 `frame_00002` 与 `frame_00003` 是
  **同一个源帧**（meta 里 `source_path` 都指向 `packages/pkg_plain/front_main/frame_00012.npz`），
  却被人工标了两次且结果不同（line 1623 px vs 2160 px）——打包缺陷，不是标注者
  与自己对同一张图"改主意"的修订链。
- 因为不存在可判定的修订继承关系，manifest 按方案 §3.1 对这类同像素不同标签
  **两侧隔离**（`resolution=isolated`），禁止按路径排序或修改时间静默覆盖权威标签。
- 完成版 `reviewed_full` 单独审计 136/136 无冲突，说明**预览包是唯一的污染源**：
  它是 20 帧交接预览的产物，早于完成版（目录时间 01:39–02:03 vs 14:39–15:49），
  且其内部就有重复槽位。测量因此显式取 `reviewed_full`。

## 3. 审计后实测（S2，协议 v5 计数契约）

权重与设备（写入报告，不是声明值）：production `logs/m5_seg/seg_model/best.pt`
sha16 `b16a734d67f3c0de`；候选 `logs/experiments/t14_e0_20260925/seed43/checkpoint_last.pt`
sha16 `aa3c615c101b3fbf`；实际设备 `cuda`。

总体（只含有标线参考的 6 个场景；micro = 整数计数汇总）：

| 模型 | C | R | 覆盖 R/C | 身份 M/R | 角色 A/L | 门（0.80/0.60/0.70） |
|---|---:|---:|---:|---:|---:|---|
| production | 380 | 366 | 0.9632 | **0.3115** | 0.7281 | 覆盖 ✓、身份 ✗、角色 ✓ |
| cand_last | 405 | 378 | 0.9333 | **0.3280** | 0.7258 | 覆盖 ✓、身份 ✗、角色 ✓ |

逐场景（production，整数计数 + 适用性）：

| 场景 | P_frames | C | R | M | L | A | 帧数 | 覆盖 | 身份 | 角色 | 判定 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| italy/ring_20260923_135218 | 6 | 25 | 19 | 14 | 14 | 9 | 6 | 0.7600 | 0.7368 | 0.6429 | R<30 样本不足 |
| italy/ring_20260923_135342 | 24 | 106 | 106 | 37 | 37 | 34 | 24 | 1.0000 | 0.3491 | 0.9189 | 通过下限 |
| italy/ring_20260923_135450 | 7 | 31 | 31 | 12 | 12 | 11 | 7 | 1.0000 | 0.3871 | 0.9167 | R<30 样本不足 |
| italy/ring_20260923_135717 | 24 | 148 | 148 | 39 | 39 | 21 | 24 | 1.0000 | 0.2635 | 0.5385 | 通过下限 |
| italy/ring_20260923_233648 | 7 | 42 | 34 | 3 | 3 | 2 | 9 | 0.8095 | 0.0882 | 0.6667 | R<30 样本不足 |
| italy/ring_20260923_233859 | 8 | 28 | 28 | 9 | 9 | 6 | 8 | 1.0000 | 0.3214 | 0.6667 | R<30 样本不足 |
| italy/ring_20260926_123638 | 0 | 0 | 0 | 0 | 0 | 0 | 28 | — | — | — | `not_applicable`（确认真无线） |
| italy/ring_20260926_123950 | 0 | 0 | 0 | 0 | 0 | 0 | 30 | — | — | — | `not_applicable`（确认真无线） |

读法（防止误读）：

- `P_frames` 是**逐帧**判断（该帧真值明确有漆线像素），不是"整组有线就全算"：
  `ring_233648` 9 帧里只有 7 帧有标线真值，另 2 帧的候选进 `C_outside_P`（只报告）。
- 覆盖率分母是 C、身份率分母是 R、角色分母是 L；**R<30 只是样本量不足**，
  不代表失败，也不允许用"C=100 但 R=1"冒充足够样本。
- 两个无线场景共 58 帧不进有线覆盖门（`not_applicable`），它们的负例/边界任务
  评价另走 `negative_line` 诊断（T10 资格：只有 verified 档位的全零标签才算合格负例）。

## 4. 与旧读数（v3）的差异归因

| 项 | v3（136 帧，旧实现） | v5 本次（136 帧，审计后） | 差异来源 |
|---|---|---|---|
| production C / R | 363 / 353 | 380 / 366 | 探针 `P_frames` 语义修正（唯一口径=真值有漆线像素，不再被"有引擎线像素"覆盖） |
| production 覆盖 / 身份 / 角色 | 0.9725 / 0.3286 / 0.7328 | 0.9632 / 0.3115 / 0.7281 | 比率改为"先累加整数再算一次"，不再逐场景比率平均 |
| cand_last 覆盖 / 身份 / 角色 | 0.9406 / 0.3407 / 0.7177 | 0.9333 / 0.3280 / 0.7258 | 同上 |

即：旧读数混用了"逐场景比率平均"和"另一套 P 帧口径"，与判定文件里的整数计数
不可比；本表是**同一套计数契约**下的读数（`candidate_metrics` 单一实现）。
`docs/CANDIDATE_GATE_CALIBRATION_20260926.md` 的旧值保留为历史，不覆盖。

## 5. 对判定与下一步的影响

- 身份率 0.31/0.33 距 0.60 很远：**当前没有可晋级候选**，与"必须至少一项因子
  真正激活的受控实验"这一 S6 目标一致（先做 E1/E2，不调低阈值）。
- 角色一致率 0.73/0.73 只是刚过 0.70，且 `ring_135717` 单场景只有 0.54——
  分场景硬门（不被总体均值掩盖）必须保留。
- `docs/W1_VERIFIED_TRUTH_136_20260926.md` §3 的 E1 方向说法已按 T15 合成实测
  更正（"权重 2.0 压 FP"被推翻）。
- 未测/未做：一致性验证（按负责人决定不做，事实保留）、预览包 22 张冲突图的
  重新标注（可选；它们不进测量分母）、最终集独立确认（R2 未开始）、Tech 驾驶。
