# 交接文档（2026-09-11，感知车道连续性攻坚轮）

## 一、状态快照

- **分支**：`p0/lane-reference-single-owner`，领先 `main` **10 个提交**，尚未并回。
  工作区干净。**未 push。**
- **验证**：`pytest tests/` **626 项全过**；`scripts/m5_offline_validate.py` **ALL PASS**。
- **游戏**：未运行（64257/64256 均不可达），**实车验收未做**。
- **未解决的环境问题**：`.git/worktrees/bn-head-check` 目录删不掉（rename/rmdir
  均"拒绝访问"，该卷 `takeown` 报不支持 ACL）。`git worktree list` 已干净、提交不受
  影响，只是 `git worktree prune` 之类会多打一行 `Permission denied`。重启后
  `rm -rf .git/worktrees/bn-head-check` 即可。

## 二、本轮最重要的事：一个数字用错了 checkpoint

**城镇场景（`m5_fsd_benchmark.py` 的 town）用 `logs/m5_seg/seg_model_v13b/best.pt`。**
此前多轮离线审计用了 `Segmenter()` 的默认（`seg_model/best.pt`），同一批 217 帧结论完全相反：

| 指标 | v13b（城镇实际） | 默认 `seg_model/best.pt` |
| --- | --- | --- |
| 近场召回 左/右 | **80.2% / 80.8%** | 19.6% / 2.2% |
| 配成双侧本车道 | 14.8% | 33.6% |
| `lane_sel=sensor` | 23.5% | 45.1% |

由此**作废**两条结论："右侧边界检不出、修法在分割侧"，以及基于默认模型的那版漏斗
百分比与提取守卫实验。**跑 `scripts/m5_lane_continuity.py` 必须显式带
`--seg-model logs/m5_seg/seg_model_v13b/best.pt`。**

仍然成立（与 checkpoint 无关）：右侧检出缺口**不存在**，训练标注也**不缺右侧**
（采集 run `R:near` 195.8 px/帧 vs `L:near` 165.3 px/帧）。

## 三、本轮实现的改动：虚线边界恢复

`beamng_autopilot/vision/lanes.py`：

- `group_world_fragments`（纯逻辑，可单测）：方向一致 `|cos| ≥ 0.94`、横向共线
  `≤ 0.45 m`、纵向间隙 `≤ 8 m`、链跨度 `≤ 60 m` 的片段并成链。
- `recover_dashed_boundaries`：每条链合成**一条长边界**。
- `_mask_fragment_polylines`：把掩码连通域回投影成世界片段。
- 接线在 `Segmenter.detect_lines`，8 项单测在 `tests/test_dashed_recovery.py`。

**为什么需要它**：城镇 `line` 类不是几条连续长线——每帧 **p50 17 个连通域**、高度
中位数 **7 px**，只有 **23.8%** 能过提取器的形状门（`h≥18`、宽度、填充率）。被丢的
是**真漆画**（被丢像素 65% 落在 GT 上），但都是**亚米级碎屑**（被丢 `unknown` 标线
世界跨度 p50 0.60 m），不携带边界几何，于是本车道只剩一侧边界、永远配不成对。

**实测（3 集 654 帧，v13b）**：

| | 配成双侧本车道 | `lane_sel=sensor` |
| --- | --- | --- |
| 归并前 | 20.8% | 45.1% |
| **归并后** | **43.0%** | **55.5%** |

关键：归并出的边界**本身足够长**，是**凭自身几何合法**跨过
`LANE_PAIRED_VISION_MIN_SPAN_M = 6.0`——**跨度门槛没有被降低**。

**接线时的坑（已量）**：归并必须只吃**学习掩码**，不能吃
`white_mask = 学习 ∪ 经典CV亮线`。喂并集只有 23.9% 配对率，喂学习掩码才是 43.0%
——CV 并集里的纹理亮边不是漆画，会污染分组。经典 CV 通路保持"只保底不新增功能"。

## 四、不要重走的坑

1. **降 `LANE_PAIRED_VISION_MIN_SPAN_M`**：3.2 m 的短配对可以在"每个点都离车很近"
   的同时让中心偏离导航路线好几米（runs 42/47/52）。实测降它能把配对率"提"到 41.9%，
   但放行帧里有 18% 不在车道内——**聚合指标不能否证一条为尾部风险存在的守卫**。
   该门现已补单测（`tests/test_lane_fusion.py`）。
2. **放开标线提取守卫**：守卫是承重的（放开 `min_height` 会让"标线"从 3.0/帧涨到
   57.9/帧而配对率不动）。
3. **放开候选门控**：`*:side` 那些被丢标线的 GT 占比是 **0%**，门控正确。
4. **时序延续（last-good hold）**：掉线是**长时缺失**——16 段、185 帧、101.6 秒，
   **最长一段 35.6 秒**，1 秒以内缺口总计只有 0.3 秒。

## 五、下一步

1. **实车验收**（阻塞中，需启动 BeamNG.tech）：
   ```
   .venv\Scripts\python.exe scripts\m5_fsd_benchmark.py --attach --runtime tech --scenarios town
   ```
   看 `lane=` 是否上升、`stall` 是否下降、`crossC/crossR/off` 是否仍为 0。
   **虚线恢复落在感知路径上，实车通过前不应视为已接受。**
2. **归并后的新漏斗**：`--diagnose` / `--gt-audit` 那套是归并**之前**跑的，
   头号损失点很可能已经换人。
3. **归并参数扫描**：`gap_max_m` / `lat_tol_m` / `dir_min` 目前是初值。
4. **并回 main**：`git checkout main && git merge --ff-only p0/lane-reference-single-owner`

## 六、本轮顺带修的无关 bug

`scripts/m5_vis_line_debug.py` 把相机分辨率写死成 `camera_model(400, 300)`，而当前
影子集是 536×403——`cx` 会算成 200 而不是 268，`fx` 同比例错，**它画的世界标线与
车道中心十字一直系统性偏移**。已改为读 episode meta 的 `cam_w/cam_h`（并与实际帧尺寸
交叉校验）。

---

## 七、实车验收结果（2026-09-11，BeamNG.tech 64257 在线）

**城镇场景全部 FAIL，且今天出现了历史基线从未有过的安全违规。四次运行汇总：**

| 运行 | 车道率 | 里程 | stall | crossC | off_road | 倒车 |
| --- | --- | --- | --- | --- | --- | --- |
| 本会话（虚线恢复 ON） | ~2.6% | 19.6 m | 182 | 0 | 0 | 11 |
| 本会话（恢复 OFF） | 17.7% | 35.0 m | 172 | 1 | 3 | 15 |
| 并行会话 #1 | 24.3% | 39.7 m | 169 | 2 | 3 | 24 |
| 并行会话 #2 | 8.1% | 32.3 m | 170 | 0 | 8 | 1 |
| **2026-09-07 历史** | 11% | **75.6 m** | **79** | 0 | 0 | 0 |

### 已定位的机制：`stale sensor`

今天的运行里 `reason=stale sensor` 出现在 **74/223 帧**（09-07 是 **0/217**），
`budget_skips` 全程为空——**不是 tick 预算延迟**。感知新鲜度门判定快照过期 →
`level=degraded`（74 帧）/ `source=none`（59 帧）→ 失败关闭停车 → stall 172。
历史运行 `source=fsd` 213 帧、`level=safe` 214 帧。

**tick 耗时不是原因**：今天 `tick_wall` p50=197ms / p90=404ms，09-07 是
p50=215ms / p90=468ms——今天反而更快。出生点也相同（779.3, 733.8）。

### 虚线恢复已被默认关闭（commit `390f158`）

A/B 显示它**压低**线上车道率（17.7% → ~0–3%），且单帧成本 ~70 ms
（`detect_lines` 端到端 64.7 → 144.3 ms），占整个分割阶段近一半。离线收益
（配对率 20.8% → 45.6%）是真实的，但过不了线上的时间预算。现改为
`BEAMNG_DASHED_RECOVERY=1` 显式开启，代码与 10 项单测保留（修法应是**让它变便宜**：
限制回投影的连通域数量、把两两归并向量化）。

### 尚未定位的回归（最高优先）

**它不是我这次改动造成的**：恢复关闭时同样失败，并行会话两次也失败。
嫌疑最大的是本轮之前那笔**车道参考抽取重构 `9d0e0c6`**——它是 09-07 与今天之间
唯一改动 FSD tick 行为的提交。但这是**假设，未证实**。

决定性实验：在重构前的 `62c8d06` 上跑同一场景，比较 `stale sensor` 帧数。
**当前被阻塞**：09:41 起有另一个进程正在跑 `m5_fsd_benchmark`，切提教会把代码从它
脚下换掉，也会抢车。等它结束后再做。

## 八、A/B 结论：`stale sensor` 回归**不是**车道参考抽取重构引入的

在重构前的 `62c8d06` 上、同一场景、同一在线 Tech 实例跑了一次（09:52–09:55）：

| 运行（同为 2026-09-11） | `stale sensor` 帧 | 里程 | 压线 | 出路面 | 倒车 |
| --- | --- | --- | --- | --- | --- |
| **重构前 `62c8d06`** | **76** / 222 | **70.2 m** | 3 | 5 | 4 |
| 重构后（本会话，恢复 OFF） | 74 / 223 | 35.0 m | 1 | 3 | 15 |
| 重构后（并行进程） | 75 / 223 | 32.3 m | 0 | 8 | 1 |
| **2026-09-07 历史** | **0** / 217 | 75.6 m | 0 | 0 | 0 |

**两个独立结论：**

1. **`stale sensor` 与代码版本无关**：重构前 76、重构后 74–75，几乎相同；而 09-07 是 0。
   所以这是**环境性**回归（游戏/仿真/传感器状态），**不是** `9d0e0c6` 引起的。
   → 我此前把嫌疑指向 `9d0e0c6` 是**错的**，已由测量否证。
2. **但"走得远不远"确实与重构相关**：同样环境下重构前 70.2 m，重构后 32–40 m。
   这是**另一条独立线索**（不是 staleness 机制，因为两者 stale 数相同），需要单独查。

另外：重构前的今天也出现 crossC 3 / 出路面 5 / 倒车 4（09-07 全为 0），进一步说明
**今天的环境本身就会产生安全违规**，与代码版本无关。

### 因此当前的优先事项

- **先查环境**：为什么今天（无论哪版代码）感知快照会过期 ~75 帧、并伴随安全违规，
  而 09-07 是 0。检查方向：仿真/传感器是否在墙钟上变慢、Tech 实例状态、
  以及 `_perception_freshness` 的阈值在今天的 cadence 下是否过紧。
- **再查重构后的进度退化**（70.2 → 32–40 m）：在同一天、环境相同的条件下，
  用今天的两组数据二分定位。
- **在此之前，任何"某改动让实车变好/变坏"的结论都不可信**——今天的基线本身是坏的。

---

## 九、`stale sensor` 根因确认，以及与并行进程的协同（2026-09-11 上午）

### 根因（由新加的 freshness 遥测一次定位）

用 `8303312` 新增的逐帧 `freshness` / `head_age_s` 字段跑了一轮城镇（`town_1789092137`）：

- 222 帧中 **75 帧 `stale sensor`**，其中 **75/75 都是 `range_s` 顶到最大值**；
- 样本：`range_s` = 1.411 / 1.771 / 0.820 s（阈值 0.8），而同时 `head_max_s` 只有 0.0–0.77、
  `bev_s`≈0、`lane_s`≈0.001。

即 **LiDAR 复用年龄**（`range_every_n=3`，真扫 ~280 ms、复用 ~40 ms）被算进
`max_sensor_age_s`，再与 `STALE_SNAPSHOT_S=0.8` 比较 → 每三个 tick 有一个撞线 → 约 33% 帧
判 stale → 失败关闭停车。

**注**：此前我用录制数据"排除"过这个假设，但那次的离线模型用了 `tick_wall_ms`（~0.2 s）当帧间
间隔，而真实墙钟间隔是「暂停仿真 + 规划 + step(0.75 s)」约 0.5–0.9 s。**假设是对的，我的模型错了。**

### 并行进程已在树里实现了同一修复（未提交）

`safety_monitor.py`（mtime 10:04/10:16，**未提交**，HEAD 里没有 `_live_sensor_age`）新增：

- `STALE_RANGE_S = 2.0`（range 用自己的复用上界）；
- `STALE_PIPELINE_S = 1.5`（把**复合快照年龄**当流水线延迟而非传感器健康——其注释援引的
  正是今天的数据："p50 0.58 s / max 0.92 s … 217/808 ticks (27%)"）；
- `_modality_age()`（排除 range 的模态年龄）。

### 我尝试过的一个补丁已撤回

我一度判断"`_live_sensor_age` 的第一个输入 `snapshot_age_s` 仍是含 range 的聚合，导致泄漏"，
并在 `fsd_drive.py` 里改了 `_sensor_snapshot_age`。**随后发现对方用 `STALE_PIPELINE_S` 正面处理了
复合年龄**，我的改法会让那道门永远无法触发（越权覆盖其设计）。**已 `git checkout` 撤回**，
`fsd_drive.py` 回到我提交的版本。

### 当前实车状态：失败模式已转移，尚未通过

- 修复前（10:02）：stale 75/222，`level` = safe 144 / degraded 75 / minimal_risk 3，里程 33.9 m。
- 带对方修复（10:10）：stale 72/221，里程 **57.7 m**。
- 带我的补丁（10:17，已撤回）：**stale 0/220**（目标达成），但失败换成
  **`reason='no drivable path'` 214/220、`minimal_risk`、车完全不动（0.0 m）**。

10:17 那次跑在对方 10:16:30 修改 `safety_monitor.py` **之后 1 分钟**启动，且树仍在被并发修改，
**因此那次结果不可归因**。`no drivable path` 需要在其修复稳定后重测。

### 协同结论

两进程共用一个工作区，必须遵守：**不要 `git add -A`**（会把对方在制品扫进自己的提交）、
**动同一文件前先看 mtime**、**验证跑之前确认树已静止**。我本轮已按此行事，并撤回了一次越权改动。
