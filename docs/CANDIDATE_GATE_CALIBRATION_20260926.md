# 候选口径门槛标定（2026-09-26，基于 136 帧权威人工真值集）

**依据**：方案 §10.2「改变候选匹配、参考覆盖或左右角色口径后，旧阈值不能未经校准直接
复用；在独立校准数据上定新值，冻结后再搜索。**不得为当前候选过门而调低阈值**」。

**校准数据**：`review_pack_20260926` 的 136 帧权威真值（8 个采集组；用户 2026-09-26
决定"按我标的作为权威"）。参考来自**数据集自己的 `label == 2`**（人工真值），
与训练标签无关。探针：`m5_marking_identity_probe`（投影候选匹配）。
可复现命令与原始 JSON 见文末。

## 1. 实测分布

| 模型 | 场景口径 | 候选数 | 有参考 | 覆盖率 | 身份率 | 左右角色 |
| --- | --- | --- | --- | --- | --- | --- |
| 生产 `seg_model/best.pt` | 全部 8 场景 | 983 | 445 | 0.4527 | 0.3101 | 0.7464 |
| 生产 | **仅有标线参考的 6 场景** | 486 | 445 | **0.9156** | 0.3101 | 0.7464 |
| 候选 `seed43/checkpoint_last.pt` | 全部 8 场景 | 1167 | 455 | 0.3899 | 0.3165 | 0.7431 |
| 候选 | **仅有标线参考的 6 场景** | 511 | 455 | **0.8904** | 0.3165 | 0.7431 |

逐场景（覆盖率 / 身份率 / 候选数）：

| 场景 | 有标线参考 | 生产 | 候选 |
| --- | --- | --- | --- |
| `135218`（wide） | 是 | 0.742 / 0.783 / 31 | 0.450 / 0.500 / 40 |
| `135342`（town） | 是 | 1.000 / 0.354 / 130 | 1.000 / 0.351 / 131 |
| `135450`（curve） | 是 | 1.000 / 0.389 / 36 | 1.000 / 0.395 / 38 |
| `135717`（plain） | 是 | 1.000 / 0.250 / 184 | 1.000 / 0.295 / 183 |
| `233648`（junction2） | 是 | 0.560 / 0.095 / 75 | 0.585 / 0.146 / 82 |
| `233859`（corner） | 是 | 1.000 / 0.333 / 30 | 1.000 / 0.351 / 37 |
| `123638`（砾石土路） | **否**（人确认无线） | 0.000 / — / 218 | 0.000 / — / 377 |
| `123950`（无标线铺装） | **否** | 0.000 / — / 279 | 0.000 / — / 279 |

## 2. 三条标定结论（写进协议 v4）

1. **覆盖率的分母必须限定在"有标线参考的场景/帧"**：无标线场景（人确认无线）
   没有参考，覆盖率**天然为 0**——那不是模型的问题，是评价集没有参考。若按全部场景
   算，任何模型都会永久失败。→ 定义：`candidate_reference_coverage` = 有参考帧上的
   有参考候选 / 该帧集产生的全部候选。
   **门槛 0.80**（要求驱动：至少 4/5 的候选必须"有参考可判"，否则身份指标只覆盖少数
   候选、结论不可用）。实测 0.890–0.916 → 通过但留有余量；**没有**按当前值下调。
2. **身份率门槛维持 0.60（协议原值，不改）**：实测两个模型都是 **0.31**（逐场景
   0.095–0.783）→ **按现口径两个模型都不该晋级**。这正是"不得为过门调低"的落点：
   校准不是为了放行，而是把"未标定"换成"实测不合格"。
3. **左右角色一致率：门槛 0.70**（要求驱动：角色混淆超过 30% 时左右身份不足以支撑
   横向参考）。实测 0.743–0.746 → 通过。
4. **逐场景判身份的样本量下限：≥30 条候选**（要求驱动：低于 30 条时 p=0.5 的 95%
   区间宽约 ±0.18，单场景结论不稳）。实测有参考场景的候选数 30–377，最小 30 →
   刚好落在门槛上，说明这个下限是"够用但不高"的选择；不足 30 条的场景记
   `needs_evidence`（不进硬门，也不当通过）。

## 3. 与旧口径的关系

* 旧 `candidate_identity_rate`（G09 之前的原始 `match_rate`）分母含"该侧无参考"的
  候选；新口径把参考覆盖拆出来单列。**旧判定不能与新口径混比**（协议哈希已变）。
* `COVERAGE_GATE_FROZEN` 由 `False` 翻为 `True`：覆盖率不再是"未标定阻止晋级"，
  而是有实测依据的**硬门**（≥0.80）。
* 影响：两个现役模型在新的覆盖率门上是**通过**的（0.89/0.92），但在身份率门
  （0.60）上**不通过**（0.31）→ 晋级仍被挡，理由从"未标定"变成"实测不合格"。

## 3.5 v5 计数契约下的重测（2026-09-26 晚，修正重复与键名两处缺陷）

**两个已修缺陷**：① 标定脚本自己 glob 两棵树 -> 同一张图被数两次（159 输入 / 136 唯一 /
23 拒绝）；② 脚本读 `summary["n_frames"]`（实际键是 `frames`/`frames_processed`）-> 帧数
恒为 0（方案点名的 `frames=0`）。修后：消费**审计后唯一清单**（`DatasetManifest` 内容去重、
拒绝带理由），计数只从 `counts`（整数）读。

| 模型 | C（P 帧内候选） | R | 覆盖率 R/C | 身份率 M/R | 角色 A/L |
| --- | --- | --- | --- | --- | --- |
| 生产 `seg_model/best.pt` | 363 | 353 | **0.9725** | 0.3286 | 0.7328 |
| 候选 `seed43/checkpoint_last.pt` | 387 | 364 | **0.9406** | 0.3407 | 0.7177 |

（无标线场景的候选记 `C_outside_P`，不进覆盖率分母；这也是 v5 与 v4 的关键差别。）
门槛不变：覆盖率 0.80、角色 0.70、身份率 0.60（**实测 0.33/0.34 仍不通过**）、
逐场景下限 30（按 R 计）。

> **后续更正（2026-09-26 深夜）：** 上表 C/R 用的是当时的探针 `P_frames` 口径
> （"有引擎线像素"曾覆盖"真值有漆线像素"），且比率按逐场景平均。按协议 v5 的
> 唯一口径重测后为 **C/R = 380/366（生产）、405/378（候选）**，覆盖率
> 0.9632 / 0.9333，身份率 0.3115 / 0.3280，角色 0.7281 / 0.7258；清单同时拆成
> "测量 136/136 唯一"与"union 审计 159 -> 114 / 45 拒绝 / 24 冲突组"。
> 完整表格与差异归因见 `T14_AUDITED_TRUTH_REMEASURE_20260926.md`；本表保留为
> 历史读数，不要与新值混比。

**训练 lineage（`experiments/lineage.py`，S1）**：评价集 136 帧中 **24 帧**
（`italy/ring_20260923_135342`）与候选训练数据**同时命中训练组与内容重叠** ->
确定 in-sample；其余 112 帧与四个权重声明的训练目录**无内容重叠**。
四个权重都没有 parent/init_from 记录（`parent_unknown`），生产模型的 13 个训练目录
**全都没有地图身份** -> 组键比对是键空间差异、**不能**读作"无泄漏"（内容比对可用）。

## 4. 局限

* 校准集 136 帧 / 8 组、单复核人（用户已决定按权威使用）、**未做一致性验证**；
* 参考是人工真值，所以这里的"匹配"以复核人的画线口径为准；
* 两个模型训练数据不同（生产含土路素材 1883 帧/60 epoch；候选 20 帧/24 epoch），
  本表**不是受控对照**，只用于标定门槛分布；
* 有参考场景的候选数最小 30（corner），该场景的身份率（0.33/0.35）区间较宽。

## 5. 可复现输入

```pwsh
# 1) 先修复已标注目录的 meta（补回 cameras；否则探针无法投影）
.venv\Scripts\python.exe scripts\m5_fix_annotated_meta.py

# 2) 标定测量
.venv\Scripts\python.exe scripts\m5_identity_calibration.py `
  --model production=logs/m5_seg/seg_model/best.pt `
  --model cand_last=logs/experiments/t14_e0_20260925/seed43/checkpoint_last.pt `
  --out logs/experiments/review_pack_20260926/identity_calibration.json
```

原始数据：`logs/experiments/review_pack_20260926/identity_calibration.json`
（含逐场景候选数、覆盖率、身份率、角色一致率与 `off_road_frac`）。
