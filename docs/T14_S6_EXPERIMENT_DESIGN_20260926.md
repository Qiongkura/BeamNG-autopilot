# S6 定向学习实验设计（冻结稿，2026-09-26）

本文件是 **S6 的运行前冻结**：两臂定义、数据准入、选择规则、最小有意义收益与
非劣余量、决策规则都在跑之前写死。方案 v2 §S6 要求"运行前冻结最小有意义收益与
非劣余量"；跑完后不得改这些数字，只允许如实报告"无改善/证据不足"。

前置状态：D1 已签收（本轮补 T10/T11 并重测），S4/S5 已落地并通过测试与页面实检；
`docs/T14_AUDITED_TRUTH_REMEASURE_20260926.md` 给出审计后读数（身份率
0.3115/0.3280，远低于 0.60）。**当前无可晋级候选**，本轮实验只做定向探索。

## 1. 数据准入与隔离（先冻结，再训练）

| 角色 | 目录 | 档位 | 说明 |
|---|---|---|---|
| 训练（线通道，研究臂） | `logs/m5_seg/line_truth_agent_full_20260925/town/front_main` | agent（弱） | 两臂共有；判定记 `research_only`，不允许晋级 |
| 开发/评价（固定） | `logs/m5_seg/line_truth_agent_full_20260925/wide|plain/front_main` + 审计后人工包 | agent / human | 评价集本实验内**不变**，不得入训 |
| 困难负例候选（E1） | 待定，见 §4 | — | **不得**取自评价帧或其空间邻近段 |

纪律：`--equal-steps` 保持两臂**实测总步数相同**（不是"最接近的整数轮"）；两臂共享
起始权重、划分、优化器、有效 batch、训练步数与评价路径；一次只改一个因子；
选择 checkpoint 的规则见 §3（跑前写死，不允许"结果出来后挑更好的那一种"）。

## 2. E2：路口/弯道漏检——用**方向**而不是**幅度**做单因子实验

**原假设**：line 类漏检（FN）与假线（FP）同时存在（人工包上 recall 0.30/0.54、
身份 0.31/0.33；agent 真值集上 precision 0.26–0.30），而**唯一还没被试过的旋钮
是 FP/FN 的比值**。

**先查历史（本轮实测，防止重复实验）**：

| 历史轮 | 配置 | 因子 | 结果 |
|---|---|---|---|
| `t14_soak4h_i18/i19/i37/i38/i56` | **road-only**（`allow_road_only=true`） | `line_tversky_weight` 0.5/1.0 | 旗标照记但 line 类被整通道屏蔽 → **因子不可能生效**；5 轮全 `needs_evidence`。本轮 S5 因子门已把这类提议判 inactive 并拒训（T14 用例） |
| `t14_auto_20260925_agentline` | 真实线监督 | `line_tversky_weight: 2.0` | 候选臂 recall 0.6896 / precision 0.302 / 身份 0.1577 → rejected |
| `t14_auto_20260925_line` | 真实线监督 | `line_tversky_weight: 2.0` | 候选臂 recall 0.728 / precision 0.2617 / 身份 0.1485 → rejected |

结论：**幅度方向（1.0→2.0）已经试过两轮**，不是未测领域；再跑同一因子是重复实验。
T15 实测（`docs/T15_SEG_LOSS_DIRECTION_20260926.md`）：实现是
`TP/(TP + alpha*FP + beta*FN)`，**只有 `beta/alpha` 比值决定 FP/FN 方向**，
提高整体权重只把方向"再推一档"，而 `alpha` **目前不在训练器白名单里**
（`m5_train_seg.py` 的 `--line-tversky-alpha` 不存在 → 提议不了）。

**因此 E2 的定义（本文件冻结）**：

1. **前置代码工作**（S6 前置，不是实验本身）：把 `line_tversky_alpha` 接进
   训练器参数与 `proposer.ALLOWED_KEYS["loss_weights"]`，并加"参数真的进了训练
   命令"的接线测试（与 `--line-tversky-weight` 同一路径）。这是**唯一**允许的
   代码改动；改动本身不得与实验轮混在一起跑（先冻结配方，再跑实验）。
2. **两臂**（唯一差别 = `beta/alpha`）：baseline 保持默认
   `alpha=0.3, beta=0.7`（比值 2.33，偏向 recall）；candidate 用
   `alpha=0.5, beta=0.5`（比值 1.0，对称）。两臂 `--line-tversky-weight 1.0`
   不变、`--epochs 24 --batch 4 --lr 0.001 --equal-steps`、seeds 42/43/44。
3. **主指标**：`line_precision` 与 `candidate_identity_rate`（micro，固定开发集），
   因为当前过不去的是这两项；`line_recall` 作为**非劣**约束（不得下降超过
   -0.03，方向实验可能牺牲一点 recall）。
4. **方向预期（T15 数值）**：比值 2.33 → 1.0 会让 |g_FN|/|g_FP| 下降（更偏 precision）。
   若实测方向相反，先按 T15 的方法复算再解释，不口头解释。

## 2.1 线监督来源（研究臂）

两臂都**不加** `--ignore-line-class`；线监督来自 agent 档位标签
（`--paint-source front_main=agent_revision`）→ 判定记 `research_only`，
**不允许晋级**。要用可晋级的线标签做同样实验，需要评价集之外的新人工线标注
（与 E1 同一约束，见 §4）。

## 3. 选择规则与判定规则（跑前冻结）

* **checkpoint 选择**：每臂同时报告 `best` 与 `checkpoint_last`，主结论以
  `checkpoint_last` 为准（两臂步数相同，避免"谁训得久"混入）；`best` 只作参考，
  不用于挑结论。
* **配对**：同 seed 的两臂差值；3 个 seed 给均值与区间（t 区间，n=3 时明确标
  样本极少）。
* **最小有意义收益**：`line_recall` 配对均值 **≥ +0.03（绝对）**。
  依据：历史单段数据组成变化的观测效应 <1 点且跨 0（`t14_e0_20260925`），
  小于 3 点的变化不作为改善证据。
* **非劣余量**：`line_recall` 不得下降超过 **-0.01**；合格负例的假线帧率相对
  变差不得超过 **+20%**（防止"多画线"换 recall）。
* **结论口径**：区间跨 0 → "证据不足"（合法结果）；达标 → "支持原假设（研究臂，
  不可晋级）"；未达标且超非劣余量 → "推翻"。
* **无收益停止**：只统计**有意义且成功完成**的实验（`round_outcome ==
  meaningful_no_gain`）；无效因子/缺标注/资格失败另有原因，不算"没有提升空间"。

## 4. E1：困难负例组成——当前**受阻**，需要先补输入

**原假设**：用已确认的土路/易混淆纹理负例替换固定比例训练样本，能降低假线
（土路 28/28 误报的历史观测）。

**阻塞原因（逐条可查）**：

1. "**已确认**负例"按 T10 必须是 `verified` 档位（人工复核）的全零标签；训练侧
   现有目录的档位是 agent/engine → 不构成合格负例（T10 用例钉住）。
2. 现有 verified 负例只在人工复核包里，而那是**开发/评价帧**（E1 明文禁止
   "取自当前固定开发/校准/最终评价帧或其空间邻近段"）。
3. 因此 E1 需要的输入是：**新的人工确认负例**（非评价集、空间上远离评价集），
   或一份书面决定把 E1 降级为研究臂（用 agent 标签负例，判定 `research_only`）。

**解除条件（按性价比排序）**：① 用户对若干非评价集的土路/碎石采集做一次人工
"确认无线"标注（最小量：≥30 帧/场景，用于替换比例 20%）；② 或明确授权研究臂
版本，并按 §3 的规则同样冻结。**在此之前不跑 E1**，也不拿 E1 的"无收益"当结论。

## 5. 命令模板（跑前核对 `--help`，参数以实际实现为准）

```pwsh
[Console]::OutputEncoding = [Text.Encoding]::UTF8; .venv/Scripts/python.exe scripts/m5_seg_autoloop.py run --run-id <新run-id> --config <冻结配置.json> --dry-run
```

```pwsh
[Console]::OutputEncoding = [Text.Encoding]::UTF8; .venv/Scripts/python.exe scripts/m5_seg_autoloop.py run --run-id <已核对run-id> --config <冻结配置.json> --no-dry-run
```

E2 的提议（单因子，经 `factor_activity` 判定线监督下 active）：

```json
{"proposals": [{"candidate_id": "e2-line-tversky-2", "family": "loss_weights",
                "factor": {"line_tversky_weight": 2.0}}]}
```

## 6. 运行后必须报告

原假设 → 是否真正激活（optimizer step、权重差异、实际损失项、生效步数）→
可复现输入（含模型/数据哈希与设备）→ 实测结果（逐 seed、逐场景、C/R/M/L/A、
负例诊断、时延）→ 支持/推翻/证据不足 → 未知与下一步。**研究臂结果不得写成
"候选可晋级"**；`best` 与 `last` 分开报。

## 7. E2 第一轮实测结果（2026-09-26 深夜，研究臂）

run：`logs/experiments/t14_e2_direction_v3_20260926`（前两轮因"唯一清单键不匹配"
导致身份率静默 UNKNOWN，已修复后重跑；两轮都保留在 `logs/` 里作为缺陷证据）。
HEAD `bf5a219`；实际设备 `cuda`；两臂各 20 训练帧、**实测步数各 120**（等步数成立）。

**是否真正激活**：`factor_activity.active=True`；`applied_flags =
['--line-tversky-alpha','0.5','--line-tversky-beta','0.5']`；两臂命令逐字 diff 只有
这两个参数（`round0_commands.json`）。判定 `research_only=True`，
`round_outcome=qualification_failure`（研究臂不允许晋级——资格失败优先于"无收益"）。

**实测（3 个配对 seed，t 区间；`seeds_needed_for_effect` = 按当前 sd 判出该效应所需 seed 数）**：

| 指标 | baseline | candidate | 配对均值差 | 95% CI 半宽 | 判定 | 需要 seed |
|---|---|---|---|---|---|---|
| line_iou | [0.1307, 0.2857, 0.1287] | [0.1390, 0.2445, 0.1309] | **-0.0102** | ±0.0671 | 证据不足 | 28 |
| line_recall | [0.6653, 0.7221, 0.7030] | [0.7282, 0.6964, 0.7008] | +0.0117 | ±0.1140 | 证据不足 | 62 |
| line_precision | [0.1399, 0.3210, 0.1361] | [0.1466, 0.2737, 0.1387] | -0.0127 | ±0.0747 | 证据不足 | 23 |
| candidate_identity_rate | [0.2694, 0.3485, 0.2914] | [0.2391, 0.3217, 0.2828] | **-0.0219**（3/3 全负） | ±0.0289 | 一致变差 | 2 |
| candidate_reference_coverage | [0.4696, 0.4490, 0.4630] | [0.4577, 0.4598, 0.4434] | -0.0069 | ±0.0393 | 证据不足 | 21 |
| left_right_role_agreement | [0.8269, 0.7609, 0.8039] | [0.8864, 0.7391, 0.9024] | +0.0454 | ±0.1525 | 证据不足 | 8 |
| offroad_false_line_px | [68626, 56714, 58445] | [61930, 67438, 59690] | +1758 | ±21667 | 证据不足 | 99 |
| inference_ms_p95 | [15.76, 15.32, 20.46] | [15.75, 17.41, 18.41] | +0.01 ms | ±5.14 | 无变化 | — |

候选臂池化（micro）：`P_frames=55, C=327, R=145, M=41, L=41, A=37` →
覆盖 0.4536、身份 0.2812、角色 0.8426；`line_recall=0.7085`、`line_precision=0.1863`、
`offroad_false_ratio=0.1933`。判定 rejected（身份 <0.6、精度 <0.4、路外假线 >0.1）。
逐场景：两个场景都 `measured`（`ring_135218` R=337、`ring_135717` R=135，均过 R≥30 下限）。

**结论（按冻结规则）**：

* **幅度/方向都未达"最小有意义收益"**：line_recall 配对均值 +0.0117 < +0.03；
  line_iou/precision 为负。按 §3 的规则这是 **"证据不足"**（3 seeds 远低于所需的
  23–62 个），不是"推翻原假设"，也不是"无收益"（`round_outcome` 已按资格失败单列）。
* 唯一一致的方向是**身份率一致下降**（3/3 seed 为负，CI 半宽 0.029 < |均值| 0.022
  仅接近）：对称方向（alpha=beta=0.5）没有换来精度，反而让"候选与真值线的匹配"
  更差——**支持保留默认的 recall 偏向（0.3/0.7）**，不支持把方向改成对称。
* 时延无变化（p95 ≈ 15.8–18.2 ms，配对差 0.01 ms）。

**未知与下一步**：3 seeds 的区间宽度是结论的主要限制；要判定 recall/precision 的
真实方向需要 20+ 个 seed（按当前 sd），成本高且不保证有意义效应——**不建议**靠加
seed 硬凑显著性。更值得做的是 E1（困难负例）或先补评价集外的 verified 线标签，
让实验具备晋级资格（否则每一轮都只能记 `research_only`）。


## 8. E1 研究臂（授权与设计冻结，2026-09-27）

用户侧"评价集外 verified 负例"尚未提供；方案 §2.2 允许研究训练自动执行，故本轮
**按研究臂执行 E1**：判定记 `research_only`，不允许晋级，结论只作研究证据。

### 8.1 负例池与判据（跑前冻结）

* 来源：`collect_it2`（18 帧）+ `collect_wc`（21 帧）中"标签无未知像素、无 line 类"
  的帧，派生成两个带 lineage 的目录（`logs/m5_seg/e1_negweak_20260927/neg_{0,1}`，
  保留 exposure/位姿；空间隔离已核对：与 dev 无 50 m 内冲突）。
* **披露**：引擎标注**不提供 line 类**，所以这些帧的"无标线"是**缺失**而不是
  "确认无线"（T10）。它们**不构成已确认负例**，只能作弱负例。
* 已实测并否掉的两条"证据化"路径（记录在案，避免重走）：
  1. 经典漆料判据（黄 HSV 先验 + 亮低饱和白）**不可用**：在人工确认无漆的 60 帧上
     也命中 4.0–11.2 万像素（判据没有区分度）；
  2. 生产模型筛查（"一个线像素都没预测"）：只剩 **5 帧**（生产模型几乎每帧都画
     点东西）→ 池子不够。

### 8.2 两臂（唯一差别 = 采样配比）

| 项 | baseline | candidate |
|---|---|---|
| `--runs` | `[town, neg_0, neg_1]` | 同左 |
| `--run-weights` | `0=1.0,1=0.0,2=0.0`（0% 负例） | `0=0.8,1=0.1,2=0.1`（**20% 负例**） |
| 其余 | `--max-train-frames 20 --epochs 24 --batch 4 --lr 0.001`、seeds 42/43/44、等步数、线监督（`--paint-source …=agent_revision`） | 同左 |

两臂每轮 20 帧、步数相同；候选臂把 20% 采样额度换成负例（"替换固定比例"）。

### 8.3 评价与判定（跑前冻结）

* 评价集：`wide`/`plain`（agent 真值，测 recall/precision/身份）+ 评价包里
  **人工确认**的两组无标线场景（`pkg_collect_t14_collect_dirt`、
  `pkg_collect_t14_collect_plain2`，共 50 帧，T10 合格）测**假线诊断**。
* **主指标**：合格负例的假线帧率（`negative_line.false_positive_frame_rate`，
  整数相加后再算比率，逐 seed 与汇总都落判定文件）。
* **非劣约束**：`line_recall` 不得下降超过 **−0.03**。
* **解释规则（因弱负例而设）**：候选臂假线帧率比基线低 **≥0.10（绝对）** →
  "支持原假设（研究臂，不可晋级）"；在 ±0.10 内 → **"证据不足/不可归因"**
  （弱负例的标签噪声无法排除）；高 ≥0.10 → "推翻"。

### 8.4 E1 研究臂实测结果（2026-09-27）

run `logs/experiments/t14_e1_research_20260927`；因子**真实激活**
（`run_weights 0=0.8,1=0.1,2=0.1` 进了候选臂命令）；两臂各 20 训练帧、
**实测步数各 120**（等步数成立）；`research_only=True`，
`round_outcome=qualification_failure`（研究臂不允许晋级）。

| 指标 | baseline | candidate | 配对均值差 | 判定 |
|---|---|---|---|---|
| **假线帧率（150 个合格负例帧 = 50 帧 × 3 seed）** | 125/150 = **0.8333** | 142/150 = **0.9467** | **+0.1134（更差）** | 按 §8.3 规则 → **推翻**（差 ≥0.10） |
| 假线像素占比 | 0.00857 | 0.01033 | 更差 | — |
| line_recall | [0.6965, 0.6863, 0.6946] | [0.6964, 0.7088, 0.6875] | +0.0051 | 非劣约束通过（>−0.03），但无改善 |
| line_precision | [0.1221, 0.2403, 0.1061] | [0.1223, 0.1581, 0.1543] | −0.0113 | 证据不足 |
| candidate_identity_rate | [0.2670, 0.3099, 0.2674] | [0.2588, 0.3121, 0.2227] | −0.0169 | 证据不足 |
| offroad_false_line_px | [73229, 63783, 73467] | [60404, 72984, 94968] | +5959 | 证据不足 |

**结论（按跑前冻结的解释规则）**：

1. **不支持原假设**：加 20% 弱负例后，合格负例上的假线帧率反而升高 0.113
   （0.833→0.947），像素占比也升高；recall 没有改善（+0.005）。
2. **限制（必须同时写）**：负例是引擎"无 line 类"帧（缺失≠确认无线，T10），
   其标签噪声既可能稀释真实效应、也可能注入假效应；因此这条结果只能读作
   "**在当前弱负例与 20% 配比下没有减少假线**"，不能读作"困难负例没用"。
3. 两个小数据臂在合格负例上的假线帧率都在 0.83–0.95，而生产权重在同一批帧上是
   0/58——与"生产训练过土路素材"的既有混淆因素一致；E1 的可晋级版本仍需要
   评价集外的 **verified 负例**（规格见 `T14_E1_READINESS_20260926.md`）。
4. 判定 `rejected`（研究臂，identity 0.2645 < 0.6、precision 0.1449 < 0.4），
   **不可晋级**；本轮不因此调整任何阈值。
