# T07 / T08 / T09 验收台账（2026-09-24，三态口径）

口径来自计划 `docs/FINAL_INTEGRATED_PLAN_20260922.md`：§7.1 要求 **PASS / FAIL / UNKNOWN 三态**，
"必需项未测不得给总体 PASS"，且"已知失败优先报告"；§6 的阶段 C 退出门要求"具备 §7 的必测覆盖；
碰撞/禁行越界不新增，UNKNOWN 不被换成 0"。本文件**只做汇总，不产生新结论**：每一行的数字都指回原文档。

**为什么要写它**：本轮之前，三项任务的"做到哪了"散在四个文档里，且出现过自相矛盾的残留
（`BOUNDARY_EVIDENCE.md` §7 的矩阵清单、`MAP_ASSOCIATION.md` §9 的 A/B 状态、`ROUND7` §6f 的"未测关联正确率"）。
台账把"已测/未测/失败"分开写死，避免把 UNKNOWN 读成 PASS，也避免把"没测"读成"干净"。

---

## 1. T07｜真实道路边界与局部拓扑

| 计划要求的量 | 三态 | 实测值 / 证据 | 缺什么 |
| --- | --- | --- | --- |
| 场景矩阵 8 行（有/无路缘、铺装+土肩、纯土路、坡道、护栏、草边、分合流、不同尺寸路口） | **PASS** | `docs/BOUNDARY_EVIDENCE.md` §5i 末表：8 行全有实测样本（合成路缘正反例、软自然边/草边、护栏 6 帧 `GUARD_RAIL 1449 px/帧`、坡道 4 条整体 1.0–4.7%/局部最高 9.8%、路口 3 处、无铺装 6+6 帧 `ASPHALT=0`、人行道路缘 6 帧 `SIDEWALK 441 px/帧`、分合流浅角 26.2°/21.1°） | 行都只是**摆位序列**（`placed_step_replay`），不是驾驶序列 |
| 定位误差（边界位置 vs 真值） | **UNKNOWN** | 无独立几何真值，`BOUNDARY_EVIDENCE.md` §5 明写"仍缺一半证据"、§7"没有精度声称" | 需要独立真值（人工标注/离线几何），本轮没做 |
| 漏边界率 | **UNKNOWN** | 只有**发布率**（按原文档字段名引用）：`BOUNDARY_EVIDENCE.md` §5b 线一致性臂发布率 **0.50**、双边发布率 **0.00**；§5c 链臂发布率 0.50/0.00；§5e 两行 0.0/0.0；§5f 0.50（双边 0.333）；§5h 0.167/0.00；§5i 1.00/0.00 | 发布率 ≠ 相对真值的漏检率；两者不可混用 |
| 假边界率 | **UNKNOWN** | 只有**与掩码边沿的一致性**（原字段名）：§5 "铺装内但非边沿 0/0"；§5e **原始候选落掩码边沿比例** 0.251（无铺装行 0.032，到边沿距离 p50 55.6 px）；§5h 土/草 0.016/0.084 | 同上，缺真值参照 |
| 错授权（错误放行） | **PASS（结构性）** | `BoundaryEvidence` 无 `drivable/permission/authority/crossable` 字段（测试钉住）、`associate()` 只报告不融合 | 无计数可言（**没有**授权口径），因此不能声称"错授权率为 0" |
| corridor feasibility 只作必要条件 | **PASS** | 未接线（`BOUNDARY_EVIDENCE.md` §7 接线裁决），历史结论"FEASIBLE 仅几何必要条件"保留 | 接线前需单因子闭环 A/B（计划 §8.2） |
| 正反例可证伪（不发布 ≠ 发布错线） | **PASS** | 路口行/无铺装行都是"什么都没发布"而非发布错线；散点集被拒、车身下方不算边界等断言的实测来源见 §5 | — |

**总体：UNKNOWN（未达）**——场景覆盖达标，**精度类必需项未测**，按 §7.1 不得给 PASS。

---

## 2. T08｜地图辅助候选关联（影子）

| 计划要求的量 | 三态 | 实测值 / 证据 | 缺什么 |
| --- | --- | --- | --- |
| 真值确认的候选身份 | **PARTIAL** | 逐候选**引擎漆线确认**（`on_line_frac`）：`wide2` 33 候选里 **10 个**被确认（`MAP_ASSOCIATION.md` §8.1 第 4 次重复）、路口 55 候选里 **3 个**（§8.3）、两个转角 **0 个**（§8.2，该处引擎线类是斑块） | 是"引擎标注"而非**人工真值**；引擎线类变斑块（占画面 39%）时确认失效 |
| A（无先验）vs B（仅辅助关联） | **PASS（已跑）** | 同一候选集**同进程配对**三臂：`MAP_ASSOCIATION.md` §7/§8.2/§8.3 | 计划原文是"地图开/关"；本轮跑的是**方向参考**的 A/B，地图开/关的净效果由"硬拒绝落在哪"判定（下一行） |
| **减少错误关联** | **FAIL（0 处净减少）** | 路口弦臂 8 个硬拒绝**全在路外候选**（漆线 0、仅路面 0）→ 与既有道路存在性判据**重复**；直路段硬拒 0 | 要出现净减少，需要"地图拒掉、道路判据不拒、且引擎漆线否认"的候选；四点里没有 |
| 不增加 false acceptance | **PASS** | 地图臂**只减不加**：假拒绝在 `wide2` 为 0/33、路口漆线确认 0/3 被拒；分数上限 1.25，无权限字段 | — |
| 无量测时不新建参考 / 不恢复巡航 | **PASS** | 无链路→弃权且**不改分数**（B≡A）；hold 候选→弃权；`test_no_measurement_abstains_and_creates_nothing` | — |
| 错误地图必须能被拒绝 | **PASS（口径已收窄，需复核）** | 轴错 90° 仍判硬冲突（`test_a_map_across_the_road_is_still_rejected`）；**纯反向 180° 不再判错**（`sense_delta_deg` 只报告） | ⚠️ 这是本轮**唯一一处把验收口径放宽**的改动，理由见 §8.1（标线无箭头）；是否接受请复核 |
| 分岔 / 宽路 / 急弯 / 单侧线 / 过期地图 | **UNKNOWN** | 分岔=单元+单帧；急弯=两个 96°/99° 转角（`§8.2`）；宽路/单侧线/过期地图=仅单元级 stale-link 反例 | 宽路、单侧线、过期地图缺实车 |
| 不输出可执行横向中心 | **PASS（结构性）** | `AssociationResult` 无 `center/offset/lateral/target/authority/drivable/crossable`；分数不依赖自车横向位置；探针打印 "shadow only"（三条测试钉住字段名/签名/源码） | — |
| 参考选择：弦 / 弧切线 / 折线切线 | **PASS（已判定）** | 弦保留：直路与转角三臂**判定相同**（§7、§8.2）；弧切线四处被否（中位 39–91°、`<45°` 16–59%）；**折线切线在路口有害**（3/3 漆线确认候选被硬拒，§8.3） | 折线要复活必须先解决"最近折线是不是自车那条链路"的身份问题 |

**总体：FAIL（未达，且已测）**——"减少错误关联"这一必需项实测为 **0**，按计划"没证明增益的复杂模型不进入下一阶段"，
**本轮不接线**。注意这是**测出来的 FAIL**，不是"没测所以搁置"。

---

## 3. T09｜保持机制联合条件 + 有界风险预测

| 计划要求的量 | 三态 | 实测值 / 证据 | 缺什么 |
| --- | --- | --- | --- |
| 有符号车身间隙 | **UNKNOWN（实车）** | `t09_risk_live{,2}.json` 36/36 tick `gap_left_m/gap_right_m` 为 None，`unknown=['evidence_interval','first_crossing','gap_left','gap_right']`（该路段未发布双侧边界） | 需要在**有双边边界**的路段重跑 |
| 首次越界距离 / 时间 | **UNKNOWN（实车）** | 同上 36/36；单元测试给了距离/时间口径（含"不趋近无有限交点"） | 同上 |
| 停止余量 | **PASS（有数字）** | `stopping_margin_m` 36/36 非空：run1 0.500–1.045 m、run2 0.500–1.892 m；理由字段固定为"flat ground, dry asphalt, no brake build-up; deceleration is a declared constant" | 减速度是**声明常量**，不是本车实测；坡/附着/制动建立时间在适用域外 → **不是安全证明** |
| 可用证据区间 | **UNKNOWN（实车）** | `evidence.state='UNKNOWN'` 36/36（无观测年龄记录）；单元测试覆盖 fresh/history/stale/missing-age | 需要把观测年龄接进风险输出 |
| 四条联合条件与"未测不算 satisfied" | **PASS** | `hold_audit` 36/36：`enforced=False`（门默认关）、`lifetime_source='observation'`；`unknown` 与 `failed` 分开（`ROUND7` §6g） | — |
| 不确定性校准 | **PARTIAL（有前后对照）** | run1：22/36 因 `sigma_lat: 1.00 m > 0.60 m`（初值协方差被当成已测量）失败；run2：改成 `unknown`（非空 7/36，0.081–0.291），失败主因转为 `remaining path unknown` 20/36 | 仍无"预测误差 vs 名义 sigma"的校准曲线 |
| 场景：快慢 tick / 长停顿 / 远处刷新 / 零速 / 大横摆 / 路口缺界 / 障碍新出现 / expired 后重复请求 / 先越界后收敛 | **UNKNOWN** | 零速/大横摆/障碍新出现/expired=单元级；路口缺界=实车 UNKNOWN（上表）；先越界后收敛=只报 `past`，无闭环收敛；快慢 tick、长停顿、远处刷新**没有对口场景** | 需要驾驶场景，不是单元 |
| 不延长保持窗口 | **PASS** | grace 0.30 / max 0.80 未动（测试钉住），门只收缩不放开；计划"本轮不批准延长"照办 | — |

**总体：PARTIAL / UNKNOWN**——"UNKNOWN 不被换成 0"这条纪律在实车里成立（且是跑出来的），
但横向量测在实车上**全部缺测**，场景覆盖以单元级为主。

---

## 4. 接线裁决与阶段位置

- **三项都不接线**：T07 只有评估器；T08 是其验收指标实测未达；T09 的门默认关且只收缩。
  计划 §9 禁止"没有全链验收就接线"，本台账即该全链验收的现状。
- **阶段位置**：阶段 A（契约/链路）与阶段 B（影子能力）在这三项上已完成到"可判定"；
  阶段 C 的第一项（路面平面统一配对 A/B）已完成且结论是**不采纳**；**阶段 D 未进入**
  （计划要求"有证据才扩大模型或权限"）。
- **给复核者的三个明确请求**：
  1. §2 表中"错误地图"口径的收窄（180° 反向不再判错）是否接受；
  2. T07 的精度类证据要哪一种真值（人工标注 / 离线几何 / Tech annotation 复核）以解锁 PASS；
  3. T09 的横向风险要不要先在**有双边边界**的路段重跑一轮（成本低），再决定是否进入场景矩阵。
