# W1 §6.3 人工评价集：复核包交接（2026-09-26）

**状态**：复核包**已就绪，等你的复核时间**。这一项是当前唯一挡住晋级链的硬阻塞：
E1（Tversky 复验）、E3（有证据的困难负例）、覆盖率/逐场景身份门槛标定、R2 全都要它。

---

## 0. 立刻开始（三条命令）

```pwsh
[Console]::OutputEncoding = [Text.Encoding]::UTF8
Set-Location -LiteralPath 'I:\projectseamng-autopilot'

# 1) 先看会执行什么（不开窗口）：确认包、帧数、类别、输出路径
pwsh -NoProfile -ExecutionPolicy Bypass -File scriptsnnotate_review_pack.ps1 -List -Reviewer <你的名字>

# 2) 真开始（逐个包；关掉一个窗口自动进下一个；14 帧）
pwsh -NoProfile -ExecutionPolicy Bypass -File scriptsnnotate_review_pack.ps1 -Reviewer <你的名字>

# 3) 只想做某几个包
pwsh -NoProfile -ExecutionPolicy Bypass -File scriptsnnotate_review_pack.ps1 -Reviewer <你的名字> -Only pkg_town,pkg_plain
```

**键位**：`1`=line · `2`=road · `3`=背景/擦除 · `b` 画笔/填充切换 · `u` 撤销 ·
`c` 清空 · `a` 上一帧 · `s` **保存并下一帧** · `q` 退出。画完一帧按 `s` 才算数。

**输出**：写到新目录 `logs\experiments
eview_pack_20260926
eviewed\<包名>ront_main\`
（源包不动，事后按两个目录算输入/输出哈希与差异）；每帧保存时写 `meta.json`，
里面记 `annotation.reviewer`、`annotated_at`、`class_source=human_revision` 与逐帧
`classes_painted`——这就是"谁、何时、对哪些类别复核过"的查询来源。

**预填模型**：默认用 `t14_e0_20260925/seed43/checkpoint_last.pt` 预填（省时间），
但**建议对"真无线铺装路/易混淆纹理"那几帧用 `-NoPrefill` 空白画**——那几帧要测的
正是模型的错，预填会诱导你确认它画出来的假线。整包空白画也可以（只有 14 帧）。

---

## 1. 现在就能打开的东西

| 内容 | 路径 |
| --- | --- |
| 起步混合包（14 帧，六类） | `logs/experiments/review_pack_20260926/review_pack.json` |
| **可打开的标注包（按采集拆成 7 个）** | `logs/experiments/review_pack_20260926/packages/pkg_*/` |
| 逐帧工作表（含空字段） | `logs/experiments/review_pack_20260926/review_worksheet.md` / `.json` |
| 全量清单（6 类 × 20）与缺口 | `logs/experiments/review_pack_20260926/review_plan.json` |
| 逐采集队列 + 打包命令 | `.../queues/queue_*.json`、`.../build_packages.ps1` |

重新打包（若你改了选帧）：

```pwsh
pwsh -NoProfile -ExecutionPolicy Bypass -File logs\experiments\review_pack_20260926\build_packages.ps1
```

**为什么拆成 7 个包**：标注包的身份是"一次采集一个 `meta.json`（`map_name`/`source_id`）"。
起步包跨了 7 次采集，合成一个包会让身份张冠李戴（本项目有过 48 帧人工修订因丢身份被
审计拒收、**不可恢复**的教训）。每个包内 `front_main/meta.json` 的身份字段已抽查齐全。

## 2. 起步包构成（20 帧，用途是**计时 + 统一规则**）

| 类别 | 帧数 | 取材 |
| --- | --- | --- |
| 清晰漆线 | 2 | town（漆线像素最高） |
| 弯道与路口 | 4 | diverse_curve / t13_junction2 / t13_corner |
| 退化与遮挡 | 4 | wide / town / 两轮补采（阴影与背光） |
| 易混淆纹理 | 4 | plain / **新采无标线铺装路**（护栏反光、岩壁、砾石路肩） |
| 真无线铺装路 | 4 | plain / **新采无标线铺装路**（待你确认"整片区域无线"） |
| 铺装/土肩与纯土路 | 2 | **新采砾石土路**（带身份） |

每帧的"为什么选它"写在 `review_worksheet.md` 的"选帧证据"列（漆线像素、未知占比）。

**2026-09-26 两轮补采**（都为补类别缺口，无人值守，身份来自会话）：

| 采集 | 组 | 帧 | 场景（我看过预览确认） | 空间独立性 |
| --- | --- | --- | --- | --- |
| `t14_collect_dirt_20260926` | `italy/ring_20260926_123638` | 96 | **砾石土路**：灰色砾石路面、无任何标线、草肩、石墙、树影 | 与既有帧最近 229 m，0 冲突 |
| `t14_collect_plain2_20260926` | `italy/ring_20260926_123950` | 120 | **无标线铺装路**：沥青、左护栏、右岩壁、砾石路肩 | 与既有帧最近 273 m，0 冲突 |

两轮都是**四视角全程零漆线像素**（采集器自己提示"线通道零信息"）——它们的作用是
负例与"确实没有线"的证据，不是漆线正例。覆盖比 0.69 / 0.52（车走了一段又折回，
所以 30 个曝光覆盖约 40 m / 30 m 而不是 58 m），帧与帧不重复（间距≈2 m）。

**复核时请额外记一件事**：引擎调色板只有 background/road/line，**分不出土路 / 土肩 /
铺装**。土路与无标线那几帧请在 `review_worksheet.md` 的 `notes` 里写 `road_type=`
（`gravel` / `asphalt` / `shoulder`），这是方案要求的"两种道路类型分别标记"。

## 3. 统一标注规则（先按这套做 14 帧，再决定要不要改）

1. **只标你实际看过并确认的区域**。没看过的区域留未知（255），**不要**把"没标"当成
   "确定没有漆线"——未标注区域不是负例（方案 §6.2）。
2. 类别取值：`background`(0) / `road`(1) / `line`(2) / 未知(255)。漆线包括：
   左/右边缘线、中央线、白线、黄线、实线、虚线。
3. 逐帧在表里填 `verdict`：`confirm`（提议可用）/ `relabel`（我改了）/ `reject`
   （这帧不适合进评价集）/ `unsure`（需要讨论）；`regions` 写你确认过的区域
   （例如 `line_left,line_right,road_edge`）。
4. **不要**把一个目录整体标成 `human_revision`：凭证必须对应"你实际看过的帧"
   （方案 §6.1）。包内帧与工作表一一对应。
5. 遇到"看不清是否有线"的帧，标 `unsure` 并在 `notes` 写原因——这类帧正是
   "退化与遮挡"类要统计的，不是废帧。

## 4. 全量清单的缺口（方案要求显式记录，不许拿别的类别凑数）

| 类别 | 已选 | 独立组数 | 帧数缺口 | 说明 |
| --- | --- | --- | --- | --- |
| 清晰漆线 | 20 | 1 | 0 | 只有 1 个组：帧多为相邻帧，独立性弱 → 需补采同类另一组 |
| 弯道与路口 | 20 | 3 | 0 | 够 |
| 退化与遮挡 | 17 | 4 | 3 | 差 3 帧：共享池子被别的类别先分走 |
| 易混淆纹理 | 20 | 2 | 0 | 够（含新采无标线铺装路） |
| 真无线铺装路 | 20 | 2 | 0 | 够（含新采无标线铺装路） |
| 铺装/土肩与纯土路 | 20 | 1 | 0 | 新采砾石土路（旧目录仍无身份、不进评价集） |

合计 **117/120** 帧可选（2026-09-26 两轮补采把 72 提到 117），剩余 3 帧缺口在
"退化与遮挡"；4 个类别仍是"只有 1 个独立组"（清晰漆线、易混淆纹理、真无线铺装路、
土路），这些在报告里按方案要求显式记为独立性缺口。

## 5. 复核时间怎么估（方案明令先计时）

**先做这 14 帧并记下耗时**（读图、判断、画线、保存、填表各花了多久），再乘全量帧数。
方案原文禁止在没测之前承诺"十分钟审核全部 640 帧"——本文件同样不承诺。
粗略参考：若每帧 30–60 s，14 帧约 7–14 min；120 帧约 1–2 h（含休息与复查）。

## 6. 复核完成后自动解锁什么

| 解锁项 | 依赖 |
| --- | --- |
| 覆盖率门槛标定（`COVERAGE_GATE_FROZEN` 翻真） | 有 verified 档的评价帧 |
| 逐场景身份门槛标定（现在只上报不进硬门） | 同上 |
| E1（Tversky 2.0 在独立评价集上复验） | 独立 verified 评价真值 |
| E3（有证据的困难负例） | 需要"确实没有线"的 verified 证据 |
| R2（离线感知候选合格） | 上述全部 + 最终集一次确认 |

**回传后我会做的事**：用 `m5_seg_dataset_audit.py` 审计身份与覆盖 → 按档位计数
（生成/审阅/人工确认/有效评价四个计数）→ 标定门槛并升协议版本 → 再跑 E1/E3。
